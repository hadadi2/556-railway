# تدقيق شامل للمستودع — Full Repository Audit

**التاريخ:** 2026-08-27 · **الأساس:** `daa7ac3` (#249) · **المنهج:** قراءة فقط (لم يُعدَّل أي ملف سوى هذا التقرير)، كل ادعاء مرسوّ بـ`file:line` فُتح فعلاً؛ أربعة فاحصين متوازيين (أمن/جودة/معمارية-أداء/اختبارات-عمليات) ثم تحقّق يدوي من كل بند برتقالي.

**صنف الدليل:** static code review (file:line) لكل البنود + direct reproduction لرُتبة الاختبار ١ (السويت الهرمتي شُغّل فعلاً في بيئة التدقيق — النتيجة أدناه).

---

## ١. الخلاصة التنفيذية · Executive summary

المستودع في حالة صحية غير معتادة لمشروع بهذا العمر على مستوى الشيفرة: صفر `except:` عارٍ، صفر TODO متراكم (واحد فقط، موثَّق ويتدهور بصدق)، عقد عدم الاختلاق مُنفَّذ بنيوياً ومحروس بـ٣٬٩٩٩ اختباراً هرمتياً و١٢٢ حارس انحدار، ومسار المال (الحجز الذرّي، fail-closed) نظيف بالكامل. لكن الخطر الأول **تشغيلي لا شيفري، وهو حرج 🔴: GitHub Actions ميت كلياً منذ 2026-08-17** — كل تشغيلات `CI` و`e2e-live-shape` على كل الفروع (بما فيها `main`) تفشل خلال ٤–٦ ثوانٍ بلا أي سجلّ منذئذٍ (آخر أخضر على `main`: 2026-07-30)، أي أن **كل دمجات آخر ١٨ يوماً (#217 تقريباً حتى #249) مرّت بلا أي تحقّق CI فعلي**، وأن الفحوص «المطلوبة» غير مُنفَذة في حماية الفروع (دُمجت PRs وفحوصها حمراء). البصمة (فشل فوري + سجلات 404 عبر كل الـworkflows) تشير لاستنفاد دقائق/سقف إنفاق Actions أو عطل فوترة — لا يُقرأ من المستودع ويحتاج فعل مالك في إعدادات الحساب. بعده تأتي ديون الشيفرة الثلاثة: **(أ)** مهلة البعثات ناعمة — بعثة بطيئة تُوسَم timed out بينما خيطها المهجور يواصل حرق نداءات مدفوعة تُهدَر نتيجتها؛ **(ب)** سباكة SQLite مكرّرة ٤ مرات وإصلاح «database is locked» (busy_timeout) وصل نسخة المنصّة فقط — مخازن المسار الرئيسي معرّضة لضياع كتابات صامت تحت التزامن؛ **(ج)** وهم تغطية في CI — ملف e2e كامل (تدفّق لغة المصنع) لا يشغّله أي مسار، ووظيفة `e2e-live-shape` يمكن أن تخضرّ بصفر خطوة متصفّح فعلية (تخطٍّ صامت عند انكسار Node/Playwright).

## ٢. خريطة المستودع · Phase-1 map

**اللغة/الإطار:** Python 3.11 · FastAPI 0.139 (مثبَّتة) · SQLite فقط (قرار مالك) · vanilla-JS بلا بناء · النشر: Railway حاوية واحدة (`Dockerfile` → `uvicorn api:app`) · CI: `.github/workflows/ci.yml` = `pytest tests/ -q` + بوابة §58، و`e2e-live-shape.yml` = رُتب ٢–٣.

| المجلد | الدور |
|---|---|
| `*.py` (جذر، ~90 وحدة، 51.8k سطر) | المحرّك كله: `api.py` (كل المسارات)، `silk_engine.py` (خط `/analyze`)، `silk_missions.py`+`silk_llm_runtime.py` (خط `/research` ذو الـ١٢ بعثة)، `silk_render.py` (العرض القانوني الواحد)، `silk_reports.py` (docx/md/pdf) |
| `silk_platform/` | منصّة المصانع متعددة المستأجرين (دراسات فقط بعد محور 2026-08-17) — auth/quota/billing/engine_bridge |
| `web/` | ثلاث صفحات مكتفية ذاتياً: `index.html` (لوحة المشغّل)، `platform.html` (المصانع)، `platform-landing.html` |
| `tests/` (337 ملفاً، 3,999 اختباراً) | هرمتي بالكامل + سجل الانحدار + إنفاذ الدروس |
| `data/` | بذور CSV مرجعية + قواعد SQLite (مُتجاهَلة git) |
| `docs/` | الحوكمة: LESSONS (~190 قيداً)، القرارات، الرؤية، runbooks |
| `migrations/` | SQL للـfact-store والمنصّة |
| `tools/` | توليد العيّنات، القبول، التصدير، بوابة §58 |
| `evals/` + `samples/` | الحالات الذهبية (فارغة بعد) + العيّنات الملتزمة (قاعدة §10.6) |
| `.claude/` | مهارات/خطاطيف الجلسة (منها بوابة «لا إنهاء والسويت أحمر») |

**تتبّع طلب كامل:** `POST /analyze` (`api.py:1070`) → `silk_engine.analyze()` (`silk_engine.py:41`): حلّ HS → ترتيب ~38 سوقاً → الوكلاء الثلاثة → طبقات الإثراء (لا تمسّ `total_score`) → الترابط → `silk_synthesis.synthesize()` (`silk_synthesis.py:105`، مسار الحكم الوحيد) → `silk_render.build_view()` (`silk_render.py:3064`، العرض الوحيد) → يُرفَق `result["view"]` وتستهلكه `web/index.html` وكل المصدّرات.

**البؤر الأكثر تغيّراً (الخطر مرجَّح عليها):** `api.py` (24 تغييراً)، `silk_ai_judge.py` (23)، `web/platform.html` (21)، `silk_platform/api.py` (19)، `silk_render.py` (18)، `silk_reports.py` (17)، `silk_quality_gate.py` (13). النتائج أدناه تتركّز فيها فعلاً.

**رُتبة الاختبار ١ (نُفِّذت — direct reproduction):** `python3 -m pytest tests/ -q` في حاوية التدقيق: **3,998 من 3,999 أخضر** بعد تثبيت عدّة الـPDF المطابقة لوصفة CI (`libreoffice-writer` + `poppler-utils` + Amiri + IBM Plex Sans Arabic). الأحمر الوحيد `tests/test_wave2_first_pdf_cluster.py::test_visual_pdf_lock_no_reversed_glyph_tokens` — عيب تصيير في **هذه الحاوية تحديداً** لا في الشيفرة: استنساخ أدنى أثبت أن انقلاب المحارف يقع فقط عند `w:bidi` + خط Plex المسمّى في تصدير PDF (نص عادٍ/عناوين/جداول سليمة، وAmiri سليم بنفس التركيبة، والخطوط ملفات أصلية مُتحقَّق منها) — وبيئة هذا الاختبار المرجعية بنصّ تخطّيه هي وظيفة `e2e-live-shape` على CI حيث يمرّ. فشلا ما قبل التثبيت (`test_factory_report_language[ar|en]`) نقص بيئة أيضاً وأخضرّا بعده.

## ٣. جدول الملاحظات · Findings table

مرتَّب بالأثر ÷ الجهد داخل كل خطورة.

| # | الخطورة | الفئة | الوصف | الدليل | الإصلاح المقترح | الجهد |
|---|---|---|---|---|---|---|
| 0 | 🔴 | عمليات/CI | **GitHub Actions ميت منذ 2026-08-17:** كل تشغيلات `CI` و`e2e-live-shape` على كل الفروع تفشل في ٤–٦ ثوانٍ بلا سجلات (٣٠+ تشغيلة متتالية على `main` وحده)؛ آخر أخضر `e63293b` 2026-07-30T14:04. كل دمجات الفترة (#245–#249 ومعها موجات إصلاح كاملة) لم يتحقّق منها أي CI، وحماية الفروع بالفحوص المطلوبة غير مُنفَذة فعلياً (الدمج تمّ والفحوص حمراء). كل بوّابات الريبو (السويت، §58، الرُتب ٢–٣) لم تنفَّذ على GitHub منذئذٍ | تشغيلات Actions: آخر أخضر run على `main` بتاريخ 2026-07-30 (~5 دقائق فعلية)؛ أول فشل فوري 2026-08-17T09:37 (`3de47fc`) وكل ما بعده ٤–٦ ثوانٍ conclusion=failure وسجلاتها 404 — عبر ci.yml وe2e-live-shape.yml معاً (فُحصت عبر GitHub API أثناء هذا التدقيق) | فعل مالك خارج المستودع: فحص فوترة/سقف إنفاق Actions في إعدادات حساب GitHub وإعادة تفعيلها، ثم تفعيل الفحصين «مطلوبَين» في حماية `main` فعلياً، ثم إعادة تشغيل CI على `daa7ac3` للتحقّق بأثر رجعي | دقائق (بعد حلّ الفوترة) |
| 1 | 🟠 | معمارية/مال | مهلة البعثة (90ث) ناعمة: `run_all_missions` ينتظر إغلاق الـpool على البعثات المهجورة، وخيطها يواصل نداءات مدفوعة تُحتسب من `SILK_RESEARCH_MAX_LLM_CALLS` وتُرمى نتيجتها؛ حلقة البعثة نفسها بلا سقف زمني جداري (حتى ١١ جولة × مهلة نداء ≤300ث) — والتعليق في الشيفرة يدّعي العكس | `silk_missions.py:654-694`، `silk_llm_runtime.py:1179-1190`، `silk_missions.py:364` | تمرير deadline رتيب داخل `_run_loop` يُفحص أول كل جولة (نفس آلية `global_cap_hit`) + اختبار | ساعات |
| 2 | 🟠 | جودة/تخزين | سباكة اتصال SQLite مكرّرة ٤×، وإصلاح «database is locked» (timeout=30 + busy_timeout=30000) وصل نسخة المنصّة وحدها؛ مخازن المسار الرئيسي على المهلة الافتراضية 5ث رغم نفس طوبولوجيا التزامن (خيوط refresh/backup/research تكتب أثناء قراءات HTTP) — write-through المرآة يُبتلَع عند القفل | `silk_storage.py:115`، `silk_store.py:73`، `silk_usage.py:64`، `silk_watchdog.py:69`، `silk_ops_log.py:47` مقابل `silk_platform/db.py:51-56`؛ ابتلاع الكتابة `silk_data_layer_v2.py:106-108`؛ الحادثة الأم `docs/PLATFORM_ROADMAP.md:558` | مُوصِّل واحد مشترك `_sqlite_connect(path, *, fk=False)` أو نسخ سطرَي الـPRAGMA للخمسة + اختبار قفل هرمتي | دقائق–ساعات |
| 3 | 🟠 | اختبارات/CI | ملف e2e يتيم: `tests/test_rung2_factory_language_flow.py` (تدفّق لغة المصنع كاملاً) موسوم `e2e` لكنه غائب عن سطر تشغيل `e2e-live-shape` — لا يشغّله أحد | `tests/test_rung2_factory_language_flow.py:36`، `tests/conftest.py:258-271`، `.github/workflows/e2e-live-shape.yml:73` | إضافة الملف لسطر pytest في الوظيفة | دقائق |
| 4 | 🟠 | اختبارات/CI | رُتبة ٣ تتخطّى (لا تفشل) داخل وظيفتها المخصّصة إن انكسر حلّ Node/Playwright — `e2e-live-shape` خضراء ممكنة بصفر خطوة متصفّح؛ نمط `SILK_PDF_ACCEPTANCE` (تخطٍّ→فشل) موجود في ملفات الـPDF ولم يُطبَّق هنا | `tests/test_rung3_playwright_e2e.py:165-180` مقابل `tests/test_report_output_overhaul.py:358-367`؛ `.github/workflows/e2e-live-shape.yml:65-68` | تحويل تخطّيات node/playwright إلى `pytest.fail` عند `SILK_RUN_E2E=1`، أو تأكيد `0 skipped` في الوظيفة | ساعة |
| 5 | 🟠 | جودة/عقد | «المستخلِص المتين الواحد» لِـJSON النموذج (قانون البند ٦) موجود ثلاث نسخ، وإصلاح fence الحيّ أُعيد تطبيقه نسخةً نسخة (docstring يعترف بالمرآة) | `silk_llm_runtime.py:801-815`، `silk_ai_judge.py:200-220` (+`:197` و`silk_llm_runtime.py:747` regex مكرّر)، `silk_product_intake.py:58-77` | مستخلِص واحد بمستوى الوحدة (مثلاً `silk_json.py`) تستورده المواقع الثلاثة كسولاً — لا يمسّ عدم-تقسيم `silk_ai_judge` | ساعات |
| 6 | 🟠 | معمارية/أداء | تحديث مؤشرات البنك الدولي يفتح اتصالاً ومعاملة **لكل صف** (~20k صف للمؤشر الواحد) على قرص Railway، بينما نظيره `upsert_trade_flows` مُدفَّع أصلاً؛ يتقاطع مع البند 2 فيرفع احتمال القفل | `silk_collectors.py:103-110` + `silk_store.py:167-177` مقابل `silk_store.py:201-224`؛ `per_page=20000` في `silk_collectors.py:95` | `upsert_indicators(rows)` دفعة واحدة (`executemany` بمعاملة واحدة) | ساعات |
| 7 | 🟠 | جودة/بنية | `api.py create_app()` إغلاق واحد بطول ~4,035 سطراً (36 مساراً، ~79 دالة متداخلة): `_run_research_pipeline` ~708 أسطر (8 كوميتات داخله)، `_research_impl` ~595، `regenerate_report` ~170 — منطق محرّك غير قابل للاختبار المعزول في ملف التغيير الأول | `api.py:217-4251`، `api.py:1617-2324`، `api.py:2637-3231`، `api.py:3899-4067` | استخراج ميكانيكي: `_run_research_pipeline` مكتمل الوسائط أصلاً وينقل حرفياً لوحدة `api_research.py`/`silk_research_pipeline.py`؛ ثم routers طبيعية بالمجموعات القائمة | أيام (+ سلّم الرُتب كاملاً) |
| 8 | 🟡 | معمارية/تكلفة | معظم الوكلاء وأداة `web_search` البعثية تتجاوز مسار HTTP المُقوّى كله (مباعدة/قاطع/إعادات/جلسة مجمّعة) بـ`requests.get` مفرد — انقطاع مزوّد عابر = فجوات معلنة في تقرير مدفوع كان القاطع سيمتصّها | المسار المقوّى `silk_data_layer.py:80-172`؛ المتجاوزون: `silk_websearch_agent.py:89,270`، `silk_gdelt_agent.py:58`، `silk_openalex_agent.py:64`، `silk_eurostat_agent.py:89`، `silk_faostat_agent.py:107`، `silk_maps_agent.py:53`، `silk_tariffs_agent.py:151`، `silk_gmaps.py:178-212`، وغيرها | تمرير هؤلاء عبر `_http_get`/شقيق `http_post` (نقاط patch الاختبارات تحتاج عناية) | ساعات–أيام |
| 9 | 🟡 | معمارية/مال | عدّادات البيانات/الرموز المشتركة بين ١١ خيطاً تُزاد بلا قفل (`+=` غير ذرّي) — وهي مُدخل إنفاذ السقوف وصدق `data_economics` | `silk_context.py:156-163+`؛ الاستهلاك `silk_llm_runtime.py:1185-1190`، `api.py:1678-1745` | `threading.Lock` واحد حول `count_data`/`record_llm_usage` | دقائق |
| 10 | 🟡 | معمارية/موارد | لا سقف لتزامن التشغيلات الثقيلة: كل `async_run` وكل إطلاق دراسة خيط جديد بلا فحص «قيد التشغيل»؛ وSWR يفرّخ خيطاً لكل إصابة عتيقة بلا dedup | `api.py:3192-3198`، `silk_platform/engine_bridge.py:1077-1084`، `silk_data_layer_v2.py:87,343-344`، `silk_gmaps.py:440-456` | `BoundedSemaphore` للتشغيلات + مجموعة in-flight لمفاتيح SWR | ساعات |
| 11 | 🟡 | معمارية/موارد | نموّ القرص الدائم بلا كنّاس: آثار JSONL كاملة البرومبتات، ملفات cache لا تُمحى (TTL يُفحص عند القراءة فقط)، صفوف ops/watchdog تتراكم — أول عرَض هو الأسوأ (فشل حفظ تحليل مدفوع) | `silk_trace.py:92-101`، `silk_llm_runtime.py:1214-1218`، `silk_cache.py` (لا prune)، `silk_storage.py:95-106` | كنس دوري داخل `silk_collectors.start_scheduler` القائم (حذف أقدم من N يوماً + سقف صفوف) | ساعات |
| 12 | 🟡 | أمن | مقارنة مفتاح المالك `X-Owner-Key` بـ`==` لا `hmac.compare_digest` — خلاف عقد الوقت-الثابت الذي يطبّقه الريبو نفسه على `X-API-Key` ورموز المنصّة | `silk_export_gate.py:183` مقابل `api.py:871`، `silk_platform/tokens.py:49-51` | `hmac.compare_digest(supplied, owner_key)` | دقائق |
| 13 | 🟡 | أمن | `--forwarded-allow-ips='*'` يثق بـ`X-Forwarded-For` من أي نظير — تزوير IP يلتفّ على خنق المعدّل ويسمّم سجل التدقيق إن كانت الحاوية يوماً قابلة للوصول المباشر | سطر CMD في `Dockerfile`؛ المستهلكون `api.py:889`، `silk_platform/api.py:322,442-447` | تقييد بـCIDR وكيل Railway، أو توثيق «لا وصول مباشر أبداً» صراحةً | دقائق |
| 14 | 🟡 | أمن | علم `Secure` للكوكي لا يُفرَض بإشارة الإنتاج الأخرى: نشر يضبط `SILK_PLATFORM_REQUIRE_SECRET=1` وحده يخدم كوكي الجلسة بلا Secure | `silk_platform/api.py:475-476` مع حارس الإقلاع `silk_platform/api.py:104-113` | اشتقاق `secure` من أيٍّ من إشارتَي الإنتاج | دقائق |
| 15 | 🟡 | أمن/تشغيل | قائمة تنقيح `_redact` الحرفية تنقص `WTO_TTD_API_KEY`/`WTO_API_KEY` و`MOYASAR_SECRET_KEY` — أول من يضيف مسباراً لهما يرث قيمة غير منقّحة | `silk_diagnostics.py:37-40` مقابل `silk_wto_tariff.py:46-48`، `silk_platform/payments.py:55` | إضافة الأسماء الثلاثة للقائمة | دقائق |
| 16 | 🟡 | اختبارات/CI | CI يثبّت `pytest`/`httpx`/`pymupdf` بلا تثبيت إصدار فوق requirements مثبَّتة عمداً — إصدار pytest جديد يستطيع تحمير/تغيير دلالات CI بصفر تغيير في الريبو | `.github/workflows/ci.yml:23`، `e2e-live-shape.yml:56` مقابل `requirements.txt:1-2` | تثبيتها في الـworkflow أو `requirements-ci.txt` | دقائق |
| 17 | 🟡 | عمليات | نسخ احتياطي بلا runbook استرجاع (ولا تمرين استرجاع موثَّق) — نصف نسخة احتياطية لقاعدة «لا تُحذف بياناتها أبداً»؛ الإطفاء الافتراضي نفسه صمّام مالك موثَّق لا ملاحظة | `silk_backup.py:175-190`، `api.py:329-332,3460-3480`؛ لا `restore` في `silk_backup.py` ولا `docs/DEPLOY_RAILWAY.md` | توثيق إجراء الاسترجاع + تمرين جاف واحد | ساعات |
| 18 | 🟡 | توثيق | `.env.example` ينقص ~78 متغيّراً مقروءاً في الشيفرة، منها مفاتيح حوادث المال (`SILK_RESEARCH_MAX_USD`، `SILK_RESEARCH_MAX_LLM_CALLS`، `COMTRADE_DAILY_BUDGET`، `SILK_WRITER_MAX_TOKENS`…) — ومنها فرع `DATABASE_URL` (انظر «يحتاج تأكيداً») | مسح `os.environ/getenv` مقابل `.env.example`؛ `silk_store.py:48,66-68` | ملحق موثَّق يُولَّد آلياً | ساعات (ميكانيكي) |
| 19 | 🟡 | توثيق | التوثيق الحاكم متقادم مادياً: CLAUDE.md يدّعي سويت «~5 ثوانٍ» (الحقيقة ~٧–٨ دقائق/3,999 اختباراً)، وREADME يدّعي «87 اختباراً» وجدول نقاط نهاية بلا `/research`/`/platform`/`/ops` | `CLAUDE.md:112`، `README.md:207,142,154-170` مقابل `.github/workflows/ci.yml:12` | تمريرة توثيق واحدة | ساعات |
| 20 | 🟡 | جودة | `silk_platform/api.py mount()` يكرّر نمط الإغلاق العملاق: 2,337 سطراً/56 مساراً على رابع ملفات التغيّر | `silk_platform/api.py:139-2475`؛ أطول معالج `:1287-1442` | تقسيم أولي داخل الملف `_mount_auth/_mount_studies/_mount_billing/_mount_admin` | ساعات |
| 21 | 🟡 | جودة | CRUD تحليلات/مستخدمين ميت داخل `silk_store.py` (سبع دوال بأسماء مطابقة لِـ`silk_storage` الحيّة وبمخطط مختلف) — لغم فوق فخّ التسمية الموثَّق؛ مستهلكه الشرعي الوحيد `tools/import_legacy.py` | `silk_store.py:244,272,283,299,320,340,352` مقابل `silk_storage.py:223,745,765,803` | قرار مالك: حذف الدوال أو docstring صاخب «legacy-import-only» | ساعات (بوّابة مالك) |
| 22 | 🟡 | جودة/عقد | قواعد رموز السباكة (dp/الوكلاء) معرَّفة ~7 مرات عبر سلسلة التطهير والبوّابة — انجراف واحد = بوّابة تفشل تسليمات على رموز لم يعد المطهّر يمسحها (عائلة LESSONS 57، محروسة اليوم بسجل الانحدار) | `silk_render.py:1044,1051,1405`، `silk_reports.py:2433-2541,2715-2732`، `silk_quality_gate.py:42` | وحدة ثوابت واحدة يركّب منها المطهّرون والكاشف | ساعات |
| 23 | 🟡 | جودة | كل قسم تقرير بحث مكتوب يدوياً ٢–٤ مرات (`_md_*`/`_docx_*`/client/HTML) — العائلة أنتجت ثلاث حوادث مسجَّلة (LESSONS 2/13/110)، والأقفال القائمة هي ما يبقيها صفراء | أزواج `silk_reports.py:5059/5071`، `:5214/5232`، `:5255/1896`؛ الثلاثة العليا `:4691,3831,4457` | مواصفة قسم صغيرة (عنوان/صفوف/نص فجوة) يستهلكها باعثا md وdocx — تدريجياً للأقسام الجديدة | أيام كاملةً؛ ساعات تدريجياً |
| 24 | 🟡 | جودة | `silk_contradiction.py` (قاعدة ٤ لمحرّك دراسة السوق) تحرسه الاختبارات ولا يستدعيه أي مسار إنتاجي — ضمان زائف؛ شقيقه `silk_judge_abstain` موصول SHADOW فعلاً | مستوردوه اختباران فقط؛ الإقرار النصي `silk_judge_abstain.py:44` | وصله بنمط SHADOW نفسه أو قيد تأجيل صريح في سجل القرارات | ساعات (بوّابة مالك) |
| 25 | 🟡 | معمارية | مسارات منصّة تكتب SQL مستأجَراً inline رغم إعلان `repository.py` «لا استعلام مستأجَر خارج هذه الطبقة» — كل المفحوص سليم النطاق اليوم، لكن نقطة الإنفاذ الواحدة صارت أمنية لا واقعاً | `silk_platform/repository.py:4-10` مقابل `silk_platform/api.py:874-876,905-907,994-1005,1366-1388,1825-1827` | نقل قراءات لِـhelpers أو توثيق الاستثناءات المسموحة ليحرسها اختبار AST | ساعات |
| 26 | 🟡 | أداء/تكلفة | لا cache لطلبات Serper (مدفوع بالنداء)/GDELT/OpenAlex/Eurostat/FAOSTAT/WITS — إعادة تشغيل/resume يعيد دفع استعلامات شبه مطابقة | `silk_cache.cached_get` يستعمله `silk_data_layer.py:463` و`silk_imf_agent.py:97` فقط؛ `silk_websearch_agent.py:89` بلا cache | لفّ Serper وأشباهه بـ`cached_get` بTTL قصير | ساعات |
| 27 | ⚪ | أمن | `/docs`/`/redoc`/`/openapi.json` عامة بلا مصادقة في التطبيقين — كشف سطح لا أسرار | `api.py:233`، `silk_platform/api.py:134` (لا override في المستودع) | `docs_url=None, openapi_url=None` في الإنتاج | دقائق |
| 28 | ⚪ | أمن | `TenantRepository.list()` يُقحم `where`/`order` نصّياً — آمن اليوم (كل النداءات حرفية) لكنه فخّ مستقبلي بلا إنذار من النوع | `silk_platform/repository.py:74-82`؛ النداءات `api.py:904,1061,1750` | allowlist لأعمدة الترتيب + تحذير docstring | دقائق |
| 29 | ⚪ | اختبارات | بوّابة §58 قابلة للإرضاء بقالب منسوخ (تفحص وجود الإعلان لا جودته) — بالتصميم الموثَّق؛ مسجَّلة كي لا تُحسَب بوّابة جودة | `tools/check_self_review_gate.py:27-54,77-87` | — (وعي فقط) أو فحص دلالي لاحق | — |
| 30 | ⚪ | اختبارات | مصنِّف إعادة التشغيل في CI: أمين (يفشل دوماً عند فشل أول) لكن وسمه يضجّ عند خطأ تجميع (إعادة كاملة بـ-v) وقد يخلط الفشل المتفاعل | `.github/workflows/ci.yml:31-57` | تحسين تسمية الحالتين لاحقاً | — |
| 31 | ⚪ | عمليات | CI وDocker يجلبان الخطوط حيّاً من `raw.githubusercontent.com` كل تشغيلة (curl -f يفشل بصوت — جيد؛ إعادة تسمية upstream تكسر CI والنشر معاً)؛ وDockerfile بلا `USER` ولا `HEALTHCHECK` (الفحص مفوَّض لـrailway.json) | `e2e-live-shape.yml:46-50`، `Dockerfile:18-21` وغياب USER | vendoring للخطوط (web/fonts موجود أصلاً) + USER غير جذري | ساعات |
| 32 | ⚪ | اختبارات | تأكيدات حرفية عربية هشّة أمام تغيير الصياغة (جزئياً مقصود — الملاحظات عقد) — ضريبة تحديث متوقَّعة مع كل PR صياغة | أمثلة `tests/test_smoke.py:343`، `tests/test_wave5c_reports.py:141`، `tests/test_research_export_from_view.py:98` | — (كلفة مقبولة، موثَّقة هنا) | — |
| 33 | ⚪ | جودة | وكلاء بحث الويب الأربعة متطابقون سطراً سطراً عدا القالب (~40 سطراً × 4)؛ و`esc()` مكرّرة بين الصفحتين (كلفة معمارية مقبولة — تحذير ألا تُصلَح واحدة دون الأخرى) | `silk_importers_agent.py:36-61` ≡ `silk_competitors_agent.py:38-64`؛ `web/index.html:330` و`web/platform.html:2887` | helper مشترك اختياري | ساعات (اختياري) |
| 34 | ⚪ | أداء | صغائر: `GET /analyses` بلا limit (والوسيط موجود)، `init_db()` يعاد على كل قراءة، `migrate()` في المسار الحار للمرآة، تحميل الأثر كاملاً بالذاكرة | `api.py:3570`، `silk_storage.py:554,778`، `silk_data_layer_v2.py:105`، `silk_trace.py:126-141` | memo لكل مسار كنمط `silk_platform/db.py:90-103` | ساعات |

## ٣ب. حالة المعالجة · resolution status (محدَّث 2026-08-27، أمر «fix all gaps»)

هذا القسم أُضيف **بعد** التدقيق، حين أمر المالك بإغلاق البنود. كل سطر يحمل
حالته الفعلية اليوم — فيصير المستند قابلاً للفرق مع التدقيق القادم (منحنى الدين
التقني) بدل أن يبقى لقطةً ساكنة.

| البند | الحالة | الأثر في الشيفرة |
|---|---|---|
| 0 | ✅ **مُحلّ بفعل مالك (2026-08-27)** | المالك جعل المستودع عاماً فعادت Actions للتشغيل. **وهنا بدأ العمل الحقيقي**: أوّل تشغيلةٍ حيّةٍ كشفت ديناً لم يكن أيُّ تدقيقٍ ساكنٍ ليراه — التفصيل في §٣ج. |
| ١ | ✅ مُصلَح + مقفول | `silk_llm_runtime._run_loop(wall_timeout_s=)` + `silk_missions._WALL_GRACE_S` |
| ٢ | ✅ مُصلَح + مقفول | `silk_sqlite.py` (جديد) — مُوصِّل واحد للمخازن الخمسة |
| ٣ | ✅ مُصلَح + مقفول | سطر تشغيل `e2e-live-shape` + حارس اليتيم القادم |
| ٤ | ✅ مُصلَح + مقفول | `tests/test_rung3_playwright_e2e._rung_gate` |
| ٥ | ✅ مُصلَح + مقفول | `silk_json.py` (جديد) — نسخة واحدة بدل ثلاث |
| ٦ | ✅ مُصلَح + مقفول | `silk_store.upsert_indicators` + مسارَي الجمع في `silk_collectors` |
| ٧ | ✅ **مُنفَّذ + مقفول** | `silk_research_pipeline.py` (جديد): نقلٌ حرفيّ لثماني دوالّ (٨٨٤ سطراً)؛ `create_app` من ~٤٬٠٦٠ إلى ~٣٬١٨٠ سطراً. حُقن المساعِدون الخمسة عبر `build()` — لا دورة استيراد (حارس AST) |
| ٨ | ✅ مُصلَح + مقفول | `silk_data_layer.throttled_request` + ٦ وكلاء |
| ٩ | ✅ مُصلَح + مقفول | `silk_context._counter_lock` |
| ١٠ | ✅ جزئياً + مقفول | منع تكرار خيوط SWR مُنفَّذ؛ **سقف تزامن التشغيلات الثقيلة لم يُنفَّذ** (يغيّر سلوكاً مرئياً: رفض 429 أو انتظار صامت — قرار مالك) |
| ١١ | ✅ مُصلَح + مقفول | `silk_janitor.py` (جديد، مطفأ افتراضياً) موصول بالمجدول القائم |
| ١٢ | ✅ مُصلَح + مقفول | `silk_export_gate.override_authorized` بـ`compare_digest` |
| ١٣ | ✅ مُعالَج بقرار معلَن | صار متغيّراً موثّقاً؛ الافتراض `*` **عمداً** (تضييق غير متحقَّق منه يُعيد حادثة §58 H1) |
| ١٤ | ✅ مُصلَح + مقفول | `silk_platform.api._secure_cookies` |
| ١٥ | ✅ مُصلَح + مقفول | `silk_diagnostics._redact` + ٨ أسماء جديدة |
| ١٦ | ✅ مُصلَح + مقفول | `requirements-ci.txt` في الوظيفتين |
| ١٧ | ✅ مُصلَح | `docs/DEPLOY_RAILWAY.md §٧` + `tools/verify_backup.py` (قراءة فقط) |
| ١٨ | ✅ مُصلَح + مقفول | ٨٢ متغيّراً وُثِّق + `tests/test_env_documented.py` |
| ١٩ | ✅ مُصلَح | `CLAUDE.md` (زمن السويت) + `README.md` (العدّ + جدول النقاط + CI) |
| ٢٠ | ⏸️ **مؤجَّل بقرار مالك** | مع ٧ و٢٣ |
| ٢١ | ✅ **قرار مالك: وسم لا حذف** | لافتة صاخبة في `silk_store.py` + قفل «لا مستورد إنتاجي» |
| ٢٢ | ⏸️ مؤجَّل | قواعد رموز السباكة ~٧ مواضع — محروسة اليوم بسجل الانحدار؛ التوحيد مع موجة ٢٣ |
| ٢٣ | ⏸️ **مؤجَّل بقرار مالك** | مع ٧ و٢٠ — تدريجياً للأقسام الجديدة |
| ٢٤ | ⏸️ **مفتوح — بانتظار قرار مالك** | `silk_contradiction` غير موصولة؛ **اختباراتها لا تثبت سلوكاً إنتاجياً ولا تُقرأ ضماناً** (مسجَّل في سجل القرارات) |
| ٢٥ | ⏸️ مؤجَّل | SQL مستأجَر inline — كل المفحوص سليم النطاق؛ حارس AST مع موجة ٢٠ |
| ٢٦ | ✅ مُصلَح + مقفول | ذاكرة Serper (مطفأة افتراضياً؛ الفشل لا يُخزَّن) |
| ٢٧ | ✅ مُصلَح + مقفول | إطفاء `/docs` مع `SILK_API_KEY` + مخرج `SILK_PUBLIC_DOCS=1` |
| ٢٨ | ✅ مُصلَح + مقفول | `silk_platform.repository._safe_order` |
| ٢٩ | ℹ️ وعي فقط | بوّابة §58 بالتصميم — مسجَّلة كي لا تُحسَب بوّابة جودة |
| ٣٠ | ℹ️ وعي فقط | مصنِّف إعادة التشغيل أمين (يفشل دوماً) |
| ٣١ | ⛔ **لا يُنفَّذ أعمى** | `USER` غير جذري قد يكسر الكتابة على وحدة Railway — غير قابل للتحقّق من هنا |
| ٣٢ | ℹ️ كلفة مقبولة | تأكيدات عربية حرفية — الملاحظات عقد |
| ٣٣ | ⏸️ اختياري | وكلاء البحث الأربعة المتطابقون |
| ٣٤ | ✅ جزئياً + مقفول | `?limit=N` مُنفَّذ؛ صغائر أخرى (memo لِـ`init_db`) مؤجَّلة |

**الحصيلة:** ٢٢ بنداً مُغلَقاً بشيفرة + قفل هرمتي (البند ٧ آخرها) · ٣ بقرار معلَن
(١٣، ٢١، ٣١) · ٦ مؤجَّلة بقرار مالك أو بانتظاره · ٤ للوعي فقط · والبند 0 حُلّ
بفعل المالك.

## ٣ج. ما كشفه CI الحيّ — ولم يكن التدقيق الساكن ليراه

البند 0 لم يكن «عائقاً إدارياً» يُزاح فيمضي كلُّ شيء. حين عادت Actions، مرّت
وظيفة `test` بالمسار **١٢ ← ٦ ← ٦ ← ٢ ← ٠ فشلاً** عبر خمسة أسبابٍ جذريةٍ
مستقلّة، كلٌّ منها أُعيد إنتاجه محلياً **قبل** إصلاحه. أربعةٌ منها دَينٌ حقيقيّ
سابق، وواحدٌ انحدارٌ أدخلتُه أنا في هذه الموجة نفسها وأصلحتُه:

| # | الجذر | الصنف | العلاج |
|---|---|---|---|
| أ | ذاكرةُ الطلبات (`SILK_CACHE_DIR`) خارج عزل `conftest` — مخبوءٌ ملوَّثٌ يُغيّر نتائج اختباراتٍ لا تعلم به | دَينٌ سابق | العزلُ في `tests/conftest._isolated_fact_store` |
| ب | حاجبُ الشبكة لا يُغلق الجلسة المجمّعة في نسختين من ثلاث — اتصالٌ مفتوحٌ يمرّ من فوق الحجب | دَينٌ سابق | الإغلاقُ في النسخ الثلاث + تحييدُ متغيّرات البروكسي |
| ج | ثلاث عائلاتٍ بيئية: نكهةُ استثناءٍ في مصنوعٍ ملتزَم، ودليلُ PDF مطلوبٌ في وظيفةٍ بلا `soffice`، واختبارٌ يتّكئ على شبكةٍ محيطة | دَينٌ سابق | تثبيتُ نكهة الاستثناء · `SILK_PDF_LOCAL_SKIP` + نقلُ الدليل إلى `e2e-live-shape` · قطعُ الاتّكاء |
| د | **ظِلُّ سمةٍ على الجلسة المشتركة يُبطل كلَّ ترقيعٍ لاحق** (`_session.request`) | **انحدارٌ أدخلتُه أنا** في 4da0608 | الاستعادةُ بالحذف لا بالإسناد (الدرس ٢٠٠) |
| هـ | **بوّابةُ أقواس WP-5 مُعاكِسةُ الإشارة**: ترفض المستندَ السليم وتقبل المقلوب — و`pymupdf` غائبةٌ عن الإنتاج فتتخطّى نفسَها بصمت | دَينٌ سابق (W-01/W-04) | مقياسٌ هندسيّ + `pymupdf` تبعيةَ إنتاج (الدرسان ٢٠١/٢٠٢) |
| و | **انتظارٌ يطابق رسالةَ الخطوة السابقة** فيمضي قبل أن يقع فعلُه — تشغيلتان على نفس المدى إحداهما خضراء | دَينٌ سابق | `clearToast` قبل كلّ فعل (الدرس ٢٠٣) |
| ز | **طبقةُ نصّ الـPDF مبعثرةٌ والورقةُ سليمة** — النسخُ والبحث (Ctrl+F) وقارئُ الشاشة يُخرجون `غري` بدل `غير` | دَينٌ سابق | تطبيعُ `ToUnicode` — الخريطةُ سليمةٌ والمستخرِجُ هو من يقلب (الدرس ٢٠٤) |

البند (هـ) يستحقّ وقفة: كنتُ قد بلّغتُ المالك أنّ الـPDF «مقلوبُ الأقواس فعلاً
والحارسُ محقّ». المعايرةُ نقضت ذلك — الورقةُ سليمةٌ ولقطةٌ مكبَّرةٌ تُثبِتها،
والحارسُ كان يقيس ظاهرةَ استخراج. التصحيحُ الكاملُ بأدلّته في
`docs/DEEP_RESEARCH_DECISIONS.md`.

**الخلاصةُ المنهجية:** «أخضرُ محلياً» و«أخضرُ على CI» ليسا مكافئين، وقد ثبت
ذلك خمس مرّاتٍ في موجةٍ واحدة (البند ١ من `docs/LESSONS.md` بأثرٍ جديد).

## ٤. خطة العمل · Action plan

**هذا الأسبوع (الأثر ÷ الجهد الأقصى):**

0. **إحياء GitHub Actions** (بند 0 — قبل كل شيء): فحص الفوترة/سقف الإنفاق في إعدادات الحساب، إعادة تشغيل CI على `daa7ac3`، ثم جعل `test` و`e2e-live-shape` مطلوبَين فعلياً في حماية `main`. كل ما يلي بلا هذا يبقى غير متحقَّق منه على GitHub.
1. **توحيد سباكة SQLite + busy_timeout** (بند 2) — سطرا PRAGMA للمخازن الخمسة أو مُوصِّل مشترك؛ يقفل عائلة «database is locked» قبل أن تعضّ المسار الرئيسي. دقائق–ساعات.
2. **سطر واحد في `e2e-live-shape.yml:73`** يعيد ملف لغة المصنع اليتيم إلى CI (بند 3)، ومعه تحويل تخطّيات رُتبة ٣ إلى فشل (بند 4) — يغلق وهمَي التغطية. ساعة.
3. **deadline جداري داخل `_run_loop`** (بند 1) — يوقف حرق النداءات المدفوعة للبعثات المهجورة. ساعات.
4. **قفل عدّادات `silk_context`** (بند 9) — سطور معدودة تحمي ركيزة صدق المحاسبة. دقائق.
5. **حزمة الدقائق الأمنية/الكفاءة:** `compare_digest` لمفتاح المالك (12) + أسماء `_redact` الثلاثة (15) + تثبيت pytest/httpx/pymupdf في CI (16) + `upsert_indicators` المدفَّع (6). ساعات مجتمعةً.

**بعده بالترتيب:** توحيد مستخلِص JSON (5) → كنّاس القرص الدائم (11) + سقف تزامن التشغيلات (10) → runbook الاسترجاع (17) → تمريرة التوثيق (18، 19) → علم Secure وforwarded-ips (13، 14) → تمرير الوكلاء عبر المسار المقوّى + cache لـSerper (8، 26) → حسم `silk_store` الميت و`silk_contradiction` (21، 24 — قرارا مالك) → استخراج `_run_research_pipeline` من `api.py` (7) ثم `mount()` المنصّة (20) — الأكبر جهداً، يُجدوَل كموجة مستقلة بسلّم الرُتب كاملاً.

## ٥. ما هو محكم ويستحق الحفاظ · What's done well

- **عقد عدم الاختلاق مُنفَّذ بنيوياً لا وعظياً:** `BaseAgent.run` يحيل الاستثناء تقريراً موسوماً (`silk_agents.py:135`)، الحرّاس المدفوعون/التعطيل صفريّو النداء، وكل الوكلاء الـ18 مغطّون باختبار يؤكّد `None`/`0.0` — لا استثناء واحد.
- **مسار المال نظيف:** حجز ذرّي `BEGIN IMMEDIATE` بفشل مغلق (`silk_usage.py:107-146,206-320`)، مُختبَر تحت ٨ خيوط متزامنة بحاجز، مع عزل دفتر الاختبارات بعد حادثة تلوّث حقيقية (`tests/conftest.py:33-49`).
- **أمن التطبيق:** صفر أسرار في الشجرة وتاريخ git كاملاً؛ صفر SQL مُركَّب من مدخلات؛ صفر `shell=True`؛ عزل مستأجَر عبر `TenantRepository` مفروض بعمود المالك وتتبّعتُه في ٦ مسارات حتى SQL؛ كلمات المرور bcrypt-12 مع pre-hash وحارس إقلاع يرفض إضعافها؛ رموز الاسترجاع single-use مجزّأة بمهلة؛ دخول ثابت الزمن بمضادّ تعداد؛ تنقيح الآثار تعاودياً عند الكتابة.
- **انضباط الأخطاء:** صفر `except:` عارٍ في المستودع كله؛ 415 `except Exception` جلّها بمبرّر مسمّى على السطر؛ TODO واحد فقط وموثَّق.
- **منطق CI أمين (في الملفات):** كتلة الفشل تنتهي `exit 1` دوماً، `pipefail` مضبوط، صفر `continue-on-error`، سقف 20 دقيقة صريح، ومصنِّف flake يسمّي لا يعيد حتى يخضرّ. (التحفّظ الحرج: المنطق لم يُنفَّذ على GitHub منذ 2026-08-17 — البند 0.)
- **العقود المعمارية الموثَّقة صامدة فعلاً:** `correlation.py` بلا استيراد شبكي (محروس AST)، الإثراء لا يمسّ `total_score`، كل مصدّر يشتق من `build_view` (لا تجميع تقارير في المسارات)، `engine_bridge` هو الجسر الوحيد للمنصّة، الحكم مسار واحد، صفر دورات استيراد بمستوى الوحدة عبر ~90 وحدة.
- **جهاز مؤسّسي نادر:** ~190 درساً كلٌّ بأداة إنفاذ ميكانيكية، 122 حارس انحدار، عيّنات ملتزمة تُختبَر حداثتها، وخطّاف «لا إنهاء والسويت أحمر» — الثقافة التي تجعل بقية هذا التقرير قابلة للإغلاق فعلاً.

## ٦. يحتاج تأكيداً · Needs confirmation

1. **فرع `DATABASE_URL` في `silk_store.py:48,66-68`** يبدو مخالفاً لقرار «SQLite فقط — لا Postgres» المستقر: أهو ثغرة انجراف أم seam توافق أمامي مقصود؟ يحسمه المالك/سجل القرارات.
2. **سبب موت Actions (بند 0):** البصمة (فشل فوري، سجلات 404، كل الـworkflows، بدءاً حاداً في 2026-08-17) ترجّح استنفاد دقائق/سقف إنفاق أو عطل دفع — لا يُقرأ إلا من إعدادات فوترة الحساب. (سؤال حماية الفروع الأصلي أُجيب بالدليل: الفحوص ليست مُنفَذة — دُمجت PRs وهي حمراء.)
3. **هل `SILK_REFRESH_HOURS` مفعَّل على النشر الحيّ؟** البند 6 لا يقع إلا معه (وإلا فهو «أصلِح قبل التفعيل»).
4. **CVEs للإصدارات المثبَّتة** (`requests==2.34.2`، `fastapi==0.139.0`، `uvicorn==0.50.0`) أحدث من قطع معرفة الفاحص — تشغيلة `pip-audit` تحسم. (`python-multipart==0.0.20` مُبرَّأ من CVE-2024-53981؛ `pytrends` صيانة خفيفة — إشارة سلسلة توريد لا ثغرة.)
5. **`mdLite` في `web/platform.html:2518`** يعرض ماركداون الكاتب كـHTML للمصانع بينما لوحة المشغّل تعرضه نصاً خاماً هرباً — أيّهما المقصود توحيدُه؟ (لا ثغرة XSS — الإدراج عبر `esc()`.)
6. **مقدار تجاوز البعثات المهجورة فعلياً** (بند 1) بنيويّ الإثبات؛ أثر تشغيلة إنتاج بطيئة (`stage_seconds`) يقيس حجمه قبل تسعير الإصلاح.
7. **حجم وحدة Railway مقابل معدّل نموّ الآثار** (بند 11) — غير قابل للرصد من هنا.

---

*مُنتَج بأربعة فاحصين متوازيين + تحقّق يدوي من البنود البرتقالية. لا تعديل على أي ملف مشروع سوى إضافة هذا التقرير. للمقارنة الدورية: أعِد التدقيق والفرق بين النسختين هو منحنى الدين التقني.*
