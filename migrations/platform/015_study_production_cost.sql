-- 015 — تكلفة إنتاج وحدة السوق على نموذج الدراسة (هدف الدراسة الاحترافية، البند ٢).
--
-- **الفجوة:** كل رقم متعذّر في «أرقام القرار» يعود لمدخل واحد غائب — تكلفة
-- إنتاج المصدّر نفسه. كانت تُدخل على منتج الكتالوج فقط (ترحيل 014) ولا حقل
-- لها على نموذج طلب الدراسة الذي يملؤه المصنع فعلاً؛ والدراسة قد تُنشأ بلا
-- ربط كتالوج أصلاً. القيمة اختيارية، بوحدة سوق المنتج (لتر للسوائل، كجم
-- لغيرها — `silk_economics.market_unit`)، وتغلب تكلفة الكتالوج عند الاجتماع
-- (`engine_bridge.apply_study_cost`). NULL = لم تُدخل — فجوة إدخال معلنة،
-- لا صفر ولا تقدير (عقد E-04).
ALTER TABLE studies ADD COLUMN production_cost REAL;
