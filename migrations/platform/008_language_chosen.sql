-- 008 — ختم اختيار اللغة الصريح · explicit language-choice stamp (§58 M4).
--
-- المشكلة: `users.language_preference` افتراضه المخطّطي 'en' منذ الترحيل 001
-- والميزة لم يكن لها زرّ أصلاً — فالقيمة المخزونة لا تميّز «اختار المستخدم»
-- من «افتراض جدول»، وتطبيق التفضيل عبر الأجهزة كان إمّا يقلب واجهة عربية
-- الأصل إنجليزيةً بافتراضٍ لم يختره أحد، أو لا يُطبَّق إطلاقاً.
--
-- الحل: ختمٌ زمني يكتبه مسار الاختيار الوحيد (`PATCH /platform/me/language`)
-- حصراً. NULL = لم يختر قطّ (يُتجاهل التفضيل المخزون)؛ قيمة = اختيار صريح
-- يُطبَّق على أي جهاز بلا اختيار محلي. إضافي فقط — لا مساس بأي صف قائم.
ALTER TABLE users ADD COLUMN language_chosen_at TEXT;
