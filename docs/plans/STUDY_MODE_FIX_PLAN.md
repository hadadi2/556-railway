# خطة إصلاح نمط «دراسة السوق التصديرية» — STUDY_MODE_FIX_PLAN

مبنية على `docs/audit/STUDY_MODE_AUDIT.md` (النتائج F-01…F-35). تُنفَّذ بعد موافقة المالك الصريحة على كل دفعة.

مبدأ (موحَّد؛ مراجعة المالك أ-١): نمط جديد `study` بجانب `decision`/`academic` (يبقيان كما هما). نمط `study` يُولَّد بمحرك القوالب الحتمي (P0-T) **وحده**: النص الثابت من `data/study_templates_ar.yaml`، الأرقام من `silk_study_numbers.py`، والنموذج يُستدعى فقط لملء فراغات (ب)/(ج) الناقصة بجملة أو جملتين. لا كتابة قسمية بالنموذج في هذا النمط، والملخص التنفيذي قالب حتمي (E1/E2).

## الخطوة ٠ — خط الأساس الرسمي (قبل أي كود؛ تعديل المالك ٠)
تشغيل حي واحد لحالة 090121 → ماليزيا بالنمط الحالي (`decision`، `SILK_MAX_REVIEW_CYCLES` الافتراضي)، وتسجيل: الدرجة من 10 بمعيار المرجع، التكلفة الفعلية بالمرحلة (البعثات/المحلل/التوليف/الكاتب/المراجع/التصدير — من `economics.cost_usd_by_mission` و`cost_usd_by_model` و`stage_seconds`؛ ما لا يفصله السجل الحالي يُقدَّر من عدّاد الرموز ويُوسم تقديراً)، الزمن، وأيّ من أخطاء البند 8 ظهر. التنفيذ: `tools/acceptance_run.py --product "قهوة محمصة" --market Malaysia --hs 090121` ضد النشر الحي (يحتاج `SILK_API_BASE` + `SILK_API_KEY`)؛ الحاوية الحالية بلا مفاتيح و`comtradeapi.un.org` محجوب بسياسة الشبكة (curl → 000)، فالتشغيل المحلي متعذر. النتيجة تُحفظ في `docs/audit/BASELINE_MALAYSIA_LIVE.md` مع الملفات المسحوبة (docx/md) تحت `samples/baseline_malaysia_live/`.

## ترتيب التنفيذ (تعديل المالك ٦)
النسخة الأولى القابلة للعرض = P0-T + الدفعة ١ كاملة + P2-5 (الجداول الخمسة) + P6-1 (رفض العناصر النائبة) → تشغيل حي → عرض النتيجة على المالك. بعدها تشغيل حي بعد كل دفعة مع تسجيل الدرجة والتكلفة في `docs/audit/STUDY_MODE_RUNS.md` (جدول: التاريخ، الدفعة، الدرجة، التكلفة بالمرحلة، الزمن، أخطاء البند 8).

## ملاحظة منفصلة — الفوترة (تعديل المالك ٧)
`silk_platform/billing.py:charge_metered` :64 غير مستدعى في أي موضع؛ لا خصم للعميل لكل تقرير اليوم (المحفظة تُخصم لفوترة التخزين الشهرية فقط، `jobs.run_storage_billing` :51). **لا يُغيَّر شيء فيه ضمن هذه الخطة**؛ ينتظر قرار المالك.

## P0-T — محرك القوالب اللغوية الحتمي (تعديل المالك الجوهري؛ يسبق الدفعة ٢ ويحل محل معظمها)

الهدف: النص الثابت يُنقل حرفياً من المرجع، والأرقام والكيانات تملأ فراغات مسماة، والنموذج يُستدعى فقط لفراغات النوعين (ب) و(ج) الناقصة. المرجع قبل التفكيك يُصحَّح بإضافة «دائرة الإحصاء الماليزية (تعداد 2020، التركيبة الدينية)» إلى قائمة المصادر (مذكورة في المتن وغائبة عن القائمة) — أُدخلت في النسخة المؤقتة.

الملفات:
- `data/study_templates_ar.yaml` — قالب لكل فقرة: `id`، `section`، `kind` (أ/ب/ج)، `variants` (شرط حتمي → نص)، الفراغات `{name}` بصيغ عرض (`{v_last:m1}` = «89.4 مليون دولار»، `{p:pct0}` = «19%»، `{y}` سنة، `{frac}` كسر عربي جاهز من `silk_study_numbers`).
- `data/product_knowledge/090121_MY.yaml` — نصوص الفئة (ج) للمرجع حرفياً (الكوبي، الطزاجة، الخولاني، الهيل، المحامص المحلية، JAKIM، الجهات، أسعار البن العالمية، دليل JAKIM/MIHAS). كل منتج×سوق متكرر يُبنى له ملف مرة واحدة.
- `silk_study_render.py` — يأخذ أرقام `silk_study_numbers` + سجل الادعاءات + ملف المعرفة؛ يختار النسخة البديلة بقواعد حتمية موثقة في رأس الملف؛ يملأ الفراغات؛ يستدعي النموذج فقط لفراغات (ب)/(ج) الناقصة بتعليمة «جملة أو جملتان، بصيغ المعجم فقط، دون أي رقم لم يُمرَّر»؛ الكتابة القسمية وP1-12 إعادة توليد الفراغ المعيب (نسخة مبسطة من P5-1؛ تعديل المالك ٣، منسجم مع P0-T). ملفات: `silk_study_render.py`، `silk_ai_judge.py` (نداء قصير لكل فراغ ب/ج)، `silk_study_linter.py` (الحد الأدنى: عنصر نائب، رقم بلا سند، مخالفة معجم). التغيير: الملخص التنفيذي قالب حتمي (E1/E2) لا يُولَّد بالنموذج؛ إعادة التوليد تنطبق على فراغات (ب) و(ج) فقط: فراغ يفشل الفحص يُعاد **مرة واحدة** بنفس المدخلات + الملاحظة؛ الفشل الثاني = حذف الادعاء وبند حدود. الأرقام والترويسة والجداول والفقرات (أ) لا تُمس. القبول: كاتب مزيف يعيد النائب دائماً → التسليم بدونه وبنداء إضافي واحد لكل فراغ. الاختبار: عدّ النداءات.

الاختبارات (الأهم):
1. `evals/golden_set/malaysia_coffee_fixture.json` (بيانات المرجع: السلسلة، الحصص، الأسعار، الصرف، المتطلبات، الجهات، القرار، الادعاءات «يُفاد») + `090121_MY.yaml` → نص **مطابق حرفاً بحرف** للمرجع بعد تطبيع المسافات؛ أي فرق يفشل الاختبار ويطبع موضعه (أول سطر مختلف + السياق).
2. حالة تركيبية: اتجاه هابط + HHI مركّز + قرار «إرجاء» → نص يمر بالـlinter ويستخدم النسخ البديلة الصحيحة (تأكيد على كل نسخة مختارة).
3. حالة بلا ملف معرفة → نسبة النص الحتمي ≥ 70% (تُقاس بالأحرف خارج فراغات النموذج).
4. اختبار الاتجاه المعكوس (تعديل المالك ٢): لكل قالب فيه فعل اتجاه، حالة بإشارات معكوسة تشمل «قيمة صاعدة مع سعر هابط» (dv>0، dp<0 ⇒ الكمية صاعدة بأكثر من القيمة) → الأفعال الصحيحة (ارتفع/تراجع/نما/انخفض) بلا فعل معاكس للإشارة.
5. اختبار docx (تعديل المالك ٦): العناوين الأحد عشر والتسميات العريضة داخل الأقسام («الرسوم والضرائب:»، «شهادة الحلال:»، «الميزة التنافسية للمنتج السعودي:»…) مطابقة للمرجع في docx وpdf (عبر pdftotext) لا في md وحده.

قواعد إضافية على القوالب (تعديلات المالك ١–٥):
- `{market_def}` في E1 نسخ مرتبطة بالقرار الرباعي واتجاه السلسلة: «واعدة» فقط مع (دخول/دخول مشروط) وسلسلة صاعدة؛ «سوقاً قائمة الحجم متراجعة الواردات» مع سلسلة هابطة؛ «سوقاً محدودة الجاذبية في الوقت الراهن» مع إرجاء/عدم دخول. النسخ الجديدة تدخل ملف المراجعة قبل التفعيل.
- كل فعل اتجاه في كل القوالب فراغ `{dir:...}` يُختار بإشارة الرقم من `silk_study_numbers` (ارتفع/تراجع، نما/انخفض، زيادة/تراجع)؛ لا فعل اتجاه مكتوب صلباً.
- قالب HHI يضيف `straddles_1500` و`straddles_2500` للنطاق العابر للعتبة بصياغة لا تحسم التصنيف («…بما بين {lo} و{hi} نقطة، وهو نطاق يعبر عتبة {th} نقطة فلا يُحسم تصنيف السوق من الحصص المرصودة وحدها؛ ويُستوضح ترتيب الموردين من المستورد المحلي قبل الاعتماد على قراءة الانفتاح أو التركّز»).
- `docs/plans/STUDY_TEMPLATE_VARIANTS_REVIEW.md`: كل نسخة بديلة لم ترد حرفياً في المرجع تُجمع فيه (القالب، الشرط، النص) بحالة `pending`؛ لا تُفعَّل نسخة قبل موافقة المالك (`approved`)؛ اختبار يمنع تحميل نسخة غير معتمدة.
- ملفات المعرفة: `tools/build_knowledge_draft.py` يولّد مسودة `data/product_knowledge/<hs>_<market>.yaml` من نتائج البعثات بحالة `status: draft`؛ فقرات (ج) تُقرأ من الملف فقط إذا `status: approved`، وإلا يكتبها النموذج وتُوسم للمراجعة. ملف ماليزيا يُكتب من المرجع بحالة `approved` بموافقة المالك.

الأثر على الخطة: P2-3 نماذج أسلوبية لفراغات (ب)/(ج) فقط (F-22). ملفات: `data/study_style_exemplars.json` (لكل فراغ: الجملة المرجعية المقابلة وصيغ المعجم المسموحة فيه)، `silk_study_render.py` (تمريرها مع تعليمة «جملة أو جملتان، بصيغ المعجم فقط، دون أي رقم لم يُمرَّر، حاكِ الأسلوب لا المحتوى»). القبول: كل فراغ يستلم نموذجه؛ لا نموذج لفقرات (أ). الاختبار: التقاط البرومبت.

### جدول تصنيف فقرات المرجع (أ = حتمي كامل، ب = قالب بفراغ نوعي، ج = معرفة المنتج×السوق)

| # | القسم | الفقرة | النوع | مصدر الفراغات / النسخ البديلة |
|---|---|---|---|---|
| H | الترويسة | المنتج/HS، المنشأ، السوق، التاريخ، أحدث سنة مكتملة | أ | أرقام القرار |
| E1 | الملخص | «تُعد السوق … نصف الواردات تقريباً» | أ | اتجاه القيمة (صعود/هبوط)؛ التعرفة (إعفاء/نسبة)؛ عدد الموردين؛ حضور السعودية (غائب/حصة)؛ تركّز أكبر ثلاثة (نصف/ثلث/هيمنة) |
| E2 | الملخص | «وبناءً على ذلك، توصي الدراسة بـ…» | أ | القرار الرباعي؛ قائمة المتطلبات المرقّمة من الفجوات (قالب لكل نوع فجوة)؛ جملة «المتطلب الأول متاح فوراً» عند فجوة تكلفة الوحدة |
| 1.1 | أولاً | السلسلة من {y_first} إلى {y_last}؛ الانكسار؛ الذروة؛ نمو آخر سنة مقابل الناتج | أ | شكل السلسلة (صعود مطرد/صعود متذبذب/هبوط/استقرار)؛ وجود نمو الناتج IMF |
| 1.2 | أولاً | تفكيك القيمة إلى كمية وسعر | أ (+ج جملة أسعار السلعة العالمية) | نافذة التفكيك؛ استبعاد آخر سنة لخلل الوزن؛ تعذّر الفصل؛ ملاحظة الإنتاج المحلي |
| 1.3 | أولاً | جدول السلسلة + سطر المصدر | أ | — |
| 1.4 | أولاً | أكبر الموردين + مركز إعادة التصدير + المنافسة الفعلية | أ (+ج توصيف الموردين: منتج منخفض التكلفة/محمّص متخصص) | عدد الموردين المعروضين؛ وجود مركز إعادة تصدير (CSV) |
| 1.5 | أولاً | مؤشر التركّز ونطاقه والعتبة | أ | دون 1,500 / 1,500–2,500 / فوق 2,500 / غير محسوب |
| 1.6 | أولاً | تغيّر حصة مورد بارز عبر سنتين + سنة أولية | أ (+ج تفسير التحوّل حسب نوع المصدّر) | وجود تغيّر ≥ 5 نقاط؛ وجود سنة أولية |
| 1.7 | أولاً | متوسط سعر الحدود وتوصيفه | أ | استبعاد سنة؛ اتجاه السعر |
| 2.1 | ثانياً | استطلاع أكاديمي بنسبه وتحفظه الببليوغرافي | ج (قشرة أ) | وجود استطلاع في الادعاءات؛ اكتمال بياناته |
| 2.2 | ثانياً | تحفظ تفسير الاستهلاك المنزلي وشريحة المنتج الصافي | ج | — |
| 2.3 | ثانياً | نمطا الاستهلاك (تقليدي/حديث) وموضع المنتج | ج | — |
| 2.4 | ثانياً | نسبة المسلمين + شرط الحلال | أ | فئة حلال ذات صلة (وإلا تُحذف) |
| 2.5 | ثانياً | الموسمية فجوة | أ | متوفرة/فجوة |
| 3.0 | ثالثاً | «رُصدت أسعار التجزئة التالية…» + الجدول | أ | صفوف ≥ 1 / لا أسعار |
| 3.1 | ثالثاً | نطاق أسعار الرف + سعر الصرف + استبعاد غير المكافئ | أ | وجود صف غير مكافئ |
| 3.2 | ثالثاً | المنافسة المحلية بالأسماء | ج (قشرة أ من الجهات role=منافس) | وجود منافسين محليين |
| 3.3 | ثالثاً | **الميزة التنافسية** | ب | القشرة حتمية حسب نوع المصدّر (محوِّل/منتج زراعي/مصنّع)؛ المحاور من المعرفة؛ شرط الجدوى وأثر التصنيف قالبان |
| 3.4 | ثالثاً | **عامل الطزاجة** | ج (+أ مدة الشحن من جدول الموانئ) | خاصية عمر رف قصير في المعرفة |
| 4.1 | رابعاً | **الرسوم والضرائب** | أ | إعفاء/نسبة؛ قاعدة الضريبة من CSV؛ اتفاقية مصدّقة جزئياً |
| 4.2 | رابعاً | جدول المتطلبات الإلزامية | أ | من CSV مع «يُستوضح» و«آخر تحقق» |
| 4.3 | رابعاً | **المتطلبات في المملكة** | أ | صفوف الخروج من CSV |
| 4.4 | رابعاً | **شهادة الحلال** | أ (قشرة) + ج (شروط المنافذ «يُفاد») | إلزامية/غير إلزامية؛ وجود هيئة حلال؛ السؤال السابق على المدة |
| 4.5 | رابعاً | **المدة الإجمالية** | أ | مدد منشورة/فجوة بجهاتها |
| 5.1 | خامساً | توصية القناة والاعتبارات الثلاثة | أ | نوع القناة؛ الاعتبارات المنطبقة (استهلاك منزلي؛ شرط حلال لدى السلاسل؛ موزع محلي) |
| 5.2 | خامساً | جدول الجهات المرصودة | أ | من الجهات المصنفة |
| 5.3 | خامساً | المستبعدون + مصادر جهات إضافية | أ (+ج أدلة السوق: JAKIM/MIHAS) | عدد المستبعدين؛ عدد المرشحين < 2 |
| 5.4 | خامساً | **اللوجستيات** | أ | الميناء؛ LPI؛ مدة الشحن؛ التكلفة فجوة |
| 6.1 | سادساً | ما تتيحه البيانات | أ | — |
| 6.2 | سادساً | الحسابات المتوقفة على تكلفة الوحدة | أ | تكلفة مُدخلة/غائبة |
| 6.3 | سادساً | القاعدة التقديرية 50–60% | أ | ثابت موسوم |
| 6.4 | سادساً | سعر الرف المستهدف غير مرصود + العتبة المؤقتة | أ | مرجع من الشريحة موجود/غائب |
| 7.1 | سابعاً | **سعر الصرف** | أ | نطاق السنوات؛ تعريف التقلب |
| 7.2 | سابعاً | **الاقتصاد الكلي** | أ | نمو/تضخم؛ تفسير بعتبات |
| 7.3 | سابعاً | **أسعار [السلعة] العالمية** | ج | مدخل خام له سعر عالمي |
| 7.4 | سابعاً | **مؤشرات الحوكمة** | أ | حاضرة (قيم + تفسير)/غائبة (كما المرجع) |
| 8.1 | ثامناً | أقوى حجة مضادة | أ (+ج جملة الذوق المحلي) | اختيار الدليل حتمياً: غياب السعودية بين N؛ حصة أكبر منتجَين (كسر)؛ مركز إعادة التصدير (كسر)؛ منافسة محلية |
| 8.2 | ثامناً | لماذا لم تُعتمد + شروط الانقلاب | ب | القشرة حسب القرار؛ شروط الانقلاب من المتطلبات؛ فراغ جملة الاستهداف |
| 9.1 | تاسعاً | جدول خطة 90 يوماً | أ | صفوف من المتطلبات + صفوف ثابتة |
| 9.2 | تاسعاً | **مؤشرات إعادة النظر** | أ | العتبة المؤقتة؛ عمر الرف؛ مدة الاعتماد |
| S1 | المصادر | قائمة المصادر | أ | من مصادر أرقام المتن |
| S2 | المصادر | ما لم يتسنّ توثيقه | أ | من الفجوات + ادعاءات «يُفاد» |
| S3 | المصادر | الجملة الختامية | أ | — |

الحصيلة: 45 فقرة/كتلة — أ: 34، ب: 2 (3.3، 8.2)، ج: 9 (2.1، 2.2، 2.3، 3.2، 3.4، 7.3، وشظايا ج داخل 1.2، 1.4، 1.6، 4.4، 5.3، 8.1). بلا ملف معرفة يبقى النص الحتمي ≈ 75% من أحرف المرجع (تقدير يُقاس بالاختبار 3).

### ثلاثة قوالب نموذجية بنسخها البديلة (بعد مراجعة المالك؛ النص الثابت حرفي من المرجع)

قواعد عامة للقوالب: كل فعل اتجاه فراغ `{dir:<key>}` يُختار بإشارة الرقم (لا فعل اتجاه مكتوب صلباً؛ اختبار يمنع ذلك). كل عدد يليه معدود يمر بمولّد العدد والمعدود (§ج). النسخ التي لم ترد حرفياً في المرجع موسومة `[pending]` وتُجمع في `docs/plans/STUDY_TEMPLATE_VARIANTS_REVIEW.md` ولا تُفعَّل قبل الموافقة.

القالب E1 (الملخص التنفيذي، الفقرة الأولى):
```
id: exec_1
kind: أ
slots: market_nisba (صفة النسبة المؤنثة من ملف جديد data/market_nisba_l1.csv (iso3, nisba_f) يُراجع يدوياً؛ countries.csv بلا عمود نسبة: الماليزية، الكويتية…),
       market (اسم السوق), product_short, v_last:m1, y_last, g_first_last:pct0, y_first,
       tariff:pct0, supplier_count:count(دولة), top1_partner, saudi_share:pct1, top3_frac
text: >
  تُعد السوق {market_nisba} {market_def}، {ease_clause}. فقد بلغت واردات {market} من هذا المنتج
  {v_last:m1} في عام {y_last}، {growth_clause}، في حين {tariff_clause}، ويتوزع الطلب على
  {supplier_count:count(دولة)} مورّدة {dominance_clause}. وفي المقابل، {saudi_clause}،
  وتستحوذ {top3_clause}.
variants:
  market_def (بالقرار الرباعي × اتجاه السلسلة):
    entry|conditional × up:   "سوقاً واعدة لـ{product_short}"                       # المرجع
    entry|conditional × flat: "سوقاً قائمة الحجم لـ{product_short}"                 # [pending]
    defer × any:              "سوقاً قائمة الحجم لـ{product_short} غير أن مؤشراتها لا تكفي لقرار دخول الآن"   # [pending]
    no_entry × any:           "سوقاً محدودة الجاذبية لـ{product_short} في الوقت الراهن"   # [pending]
    any × down:               "سوقاً متراجعة الواردات لـ{product_short}"            # [pending]
  ease_clause:
    default: "غير أنها ليست سوقاً سهلة الدخول"                                      # المرجع
    no_entry: "وليست سوقاً سهلة الدخول"                                            # [pending]
  growth_clause:
    up:   "بـ{dir:noun_up} تقارب {g_first_last:pct0} عن عام {y_first}"      # المرجع (زيادة)
    down: "بـ{dir:noun_down} يقارب {g_first_last_abs:pct0} عن عام {y_first}" # [pending] (تراجع)
    flat: "بما يعادل تقريباً مستوى عام {y_first}"                             # [pending]
  tariff_clause:
    exempt: "يُعفى المنتج من الرسوم الجمركية"                                    # المرجع
    rate:   "يخضع المنتج لرسم جمركي بنسبة {tariff:pct0}"                         # [pending]
    gap:    "لم يُتحقق من الرسم الجمركي المنطبق بعد"                              # [pending]
  dominance_clause:
    hhi_low:  "دون هيمنة أي منها"                                                 # المرجع
    hhi_mid:  "مع تركّز متوسط لدى أكبرها"                                        # [pending]
    hhi_high: "مع هيمنة واضحة لـ{top1_partner}"                                    # [pending]
    no_shares: ""  (تُحذف الجملة الاعتراضية)                                       # [pending]
  saudi_clause:
    absent:  "لا يوجد مورّد سعودي في هذه السوق حالياً"                             # المرجع
    present: "لا تتجاوز حصة المملكة {saudi_share:pct1} من هذه السوق حالياً"        # [pending]
  top3_clause:
    n>=3 & near:  "ثلاث دول قريبة جغرافياً على {top3_frac} الواردات تقريباً"   # المرجع
    n>=3 & far:   "ثلاث دول على {top3_frac} الواردات تقريباً"                     # [pending]
    n<3:   "{top_n:count(دولة)} على {topn_frac} الواردات تقريباً"                  # [pending]
    no_shares: "ولم تتوفر حصص الموردين لبيان تركّز الواردات"                      # [pending]
rules: decision من P1-5؛ اتجاه السلسلة من P1-7 (up إذا g>+5%، down إذا g<−5%، وإلا flat)؛
       hhi_low/mid/high من حدّي P1-9 (أعلى الحد < 1,500 / يعبر أو داخل 1,500–2,500 / أدنى الحد ≥ 2,500).
```
قاعدة «قريبة جغرافياً» (قرار المالك: نص المرجع لا يُمس): `near` إذا كانت مسافة العواصم بين السوق وكلٍّ من أكبر ثلاثة موردين ≤ 3,000 كم وفق `data/geodist_l1.csv` (CEPII GeoDist، `dist_cepii` عمود `distcap`، الإصدار والمصدر في رأس الملف)؛ غياب الملف أو الصف = `far` (بلا الصفة). حالة المرجع: كوالالمبور↔جاكرتا ≈ 1,180 كم، ↔سنغافورة ≈ 320 كم، ↔هانوي ≈ 2,100 كم → near. **الملف لم يُنزَّل بعد: `www.cepii.fr` محجوب بسياسة شبكة الحاوية (connect_rejected)؛ يُضاف عند فتح المضيف أو بتزويد المالك للملف.**

القالب 1.5 (مؤشر التركّز):
```
id: s1_hhi
kind: أ
slots: hhi_low:int, hhi_high:int, origin_word (إقليميين/أوروبيين/… من قرب أكبر ثلاثة موردين: pending حتى مصدر المسافات، وإلا «الرئيسيين»)
variants:
  below_1500: >                                                                   # المرجع
    ويُقدَّر مؤشر تركّز الموردين، محسوباً من الحصص المرصودة أعلاه، بما بين {hhi_low:int} و{hhi_high:int}
    نقطة من أصل 10,000، وهو ما يقل بوضوح عن العتبة التي تُصنَّف عندها السوق مركّزة (1,500 نقطة).
    وتعني هذه القراءة أن السوق مفتوحة أمام الموردين الجدد، مع منافسة سعرية فعلية من الموردين {origin_word}.
  straddles_1500: >                                                               # [pending]
    ويُقدَّر مؤشر تركّز الموردين، محسوباً من الحصص المرصودة أعلاه، بما بين {hhi_low:int} و{hhi_high:int}
    نقطة من أصل 10,000، وهو نطاق يعبر عتبة التركّز (1,500 نقطة) فلا يُحسم من الحصص المرصودة وحدها
    ما إذا كانت السوق مفتوحة أم مركّزة؛ ويُستوضح ترتيب الموردين الكامل من المستورد المحلي قبل البناء على أي من القراءتين.
  between_1500_2500: >                                                            # [pending]
    ويُقدَّر مؤشر تركّز الموردين، محسوباً من الحصص المرصودة أعلاه، بما بين {hhi_low:int} و{hhi_high:int}
    نقطة من أصل 10,000، وهو ما يقع ضمن نطاق التركّز المتوسط (1,500 إلى 2,500 نقطة).
    وتعني هذه القراءة أن الدخول ممكن، غير أن المورد الأكبر يملك وزناً تفاوضياً يتعين احتسابه.
  straddles_2500: >                                                               # [pending]
    ويُقدَّر مؤشر تركّز الموردين، محسوباً من الحصص المرصودة أعلاه، بما بين {hhi_low:int} و{hhi_high:int}
    نقطة من أصل 10,000، وهو نطاق يعبر عتبة التركّز الشديد (2,500 نقطة) فلا يُحسم من الحصص المرصودة وحدها
    ما إذا كانت السوق متوسطة التركّز أم شديدته؛ ويُستوضح ترتيب الموردين الكامل من المستورد المحلي قبل البناء على أي من القراءتين.
  above_2500: >                                                                   # [pending]
    ويُقدَّر مؤشر تركّز الموردين، محسوباً من الحصص المرصودة أعلاه، بما بين {hhi_low:int} و{hhi_high:int}
    نقطة من أصل 10,000، وهو ما يتجاوز العتبة التي تُصنَّف عندها السوق شديدة التركّز (2,500 نقطة).
    وتعني هذه القراءة أن السوق محكومة بعدد محدود من الموردين، ويتوقف الدخول على إزاحة أحدهم لا على فجوة قائمة.
  not_computed: >                                                                 # [pending]
    ولم يتسنّ احتساب مؤشر تركّز الموردين لغياب حصص الموردين في بيانات {source_gap}، ويُستوضح ذلك
    من المستورد المحلي عند بدء التفاوض؛ ولا تُقرأ هذه الفجوة سوقاً مفتوحة.
rules (بالترتيب، على حدّي P1-9):
  hhi_high < 1500                  → below_1500
  hhi_low < 1500 <= hhi_high       → straddles_1500
  1500 <= hhi_low and hhi_high < 2500 → between_1500_2500
  hhi_low < 2500 <= hhi_high       → straddles_2500
  hhi_low >= 2500                  → above_2500
  لا حصص                            → not_computed
```

القالب 1.2 (تفكيك نمو القيمة):
```
id: s1_decomp
kind: أ (+ شظية ج اختيارية: world_price_note)
slots: y_a, y_b, dv:pct0, dp:pct0, p_a:usd1, p_b:usd1, dq:pct0, q_share_frac, y_excl, span:count(سنة),
       q_growth_frac, last_yoy:pct0, commodity, local_producers_word
dirs: {dir:v} من إشارة dv، {dir:p} من إشارة dp، {dir:q} من إشارة dq؛ الصيغ: ارتفع/تراجع، ارتفاع/تراجع، نما/انكمش، صعود/هبوط
variants:
  decomposable_with_excluded_last (dv>0, dp>0, dq>0, سنة مستبعدة): >                # المرجع
    ويتعين قراءة هذا النمو بحذر، لأنه نمو في القيمة يجمع أثر الكمية وأثر السعر معاً. فبين عامي {y_a} و{y_b}
    {dir:v:ارتفعت|تراجعت} القيمة بنسبة {dv:pct0}، بينما {dir:p:ارتفع|تراجع} متوسط سعر الكيلوغرام المستورد بنسبة {dp:pct0}
    (من {p_a:usd1} إلى {p_b:usd1} دولار)، أي أن الكمية المستوردة {dir:q:ارتفعت|تراجعت} بنحو {dq:pct0} خلال الفترة،
    وتفسّر نحو {q_share_frac} نمو القيمة، ويعود الباقي إلى {dir:p:ارتفاع|تراجع} السعر. أما عام {y_excl} فلا يمكن فصل
    أثر الكمية فيه عن أثر السعر لعدم موثوقية بيانات الوزن{world_price_note}. وعليه، فإن الاستنتاج المؤكد هو أن
    الكمية المستوردة {dir:q:نمت|انكمشت} بما يقارب {q_growth_frac} في {span:count(سنة)}، لا أن {dir:v:نمو|تراجع} الطلب بلغ
    {last_yoy:pct0} في عام واحد{local_production_note}.
  decomposable_full: (النسخة نفسها بلا جملة «أما عام…»؛ النافذة حتى y_last)                       # [pending]
  value_up_price_down (dv>0, dp<0): >                                                          # [pending]
    ويتعين قراءة هذا النمو بحذر، لأنه نمو في القيمة يجمع أثر الكمية وأثر السعر معاً. فبين عامي {y_a} و{y_b}
    ارتفعت القيمة بنسبة {dv:pct0} بينما تراجع متوسط سعر الكيلوغرام المستورد بنسبة {dp_abs:pct0}
    (من {p_a:usd1} إلى {p_b:usd1} دولار)، أي أن الكمية المستوردة ارتفعت بنحو {dq:pct0} خلال الفترة، بما يفوق نمو القيمة
    نفسه{excl_note}. وعليه، فإن النمو المرصود نمو في الكمية بالدرجة الأولى، ويُقرأ بحذر لأن انخفاض السعر قد يعكس
    تحولاً نحو درجات أدنى{local_production_note}.
  value_down_price_up (dv<0, dp>0): >                                                          # [pending]
    ويتعين قراءة هذا التراجع بحذر، لأنه تراجع في القيمة يجمع أثر الكمية وأثر السعر معاً. فبين عامي {y_a} و{y_b}
    تراجعت القيمة بنسبة {dv_abs:pct0} بينما ارتفع متوسط سعر الكيلوغرام المستورد بنسبة {dp:pct0}
    (من {p_a:usd1} إلى {p_b:usd1} دولار)، أي أن الكمية المستوردة تراجعت بنحو {dq_abs:pct0} خلال الفترة، بما يفوق
    تراجع القيمة نفسه{excl_note}. وعليه، فإن الانكماش المرصود انكماش في الكمية بالدرجة الأولى{local_production_note}.
  price_led (نفس الإشارة، dq <= 0.2·dv): >                                                     # [pending]
    ويتعين قراءة هذا {dir:v:النمو|التراجع} بحذر، لأنه {dir:v:نمو|تراجع} في القيمة يجمع أثر الكمية وأثر السعر معاً.
    فبين عامي {y_a} و{y_b} {dir:v:ارتفعت|تراجعت} القيمة بنسبة {dv_abs:pct0} بينما {dir:p:ارتفع|تراجع} متوسط سعر
    الكيلوغرام بنسبة {dp_abs:pct0}، أي أن الكمية {q_change_clause}؛ فالتغير المرصود سعري في المقام الأول لا
    {dir:v:توسّعاً|انكماشاً} في الطلب{local_production_note}.
  not_decomposable: >                                                                          # [pending]
    ويتعين قراءة هذا {dir:v:النمو|التراجع} بحذر، لأنه {dir:v:نمو|تراجع} في القيمة يجمع أثر الكمية وأثر السعر معاً،
    ولا يمكن فصل أثر الكمية عن أثر السعر في هذه السلسلة {reason_clause}؛ وعليه لا يُقرأ الرقم
    {dir:v:نمواً|تراجعاً} في الطلب{local_production_note}.
sub-slots:
  world_price_note (ج من ملف المعرفة المعتمد): "، مع الأخذ في الاعتبار أن أسعار {commodity} العالمية {dir:w:ارتفعت|تراجعت} بحدة في تلك الفترة" / ""
  local_production_note: "؛ مع ملاحظة أن الواردات لا تمثل الطلب الكلي في سوق تنشط فيها {local_producers_word}" / ""
  excl_note: "، مع استبعاد عام {y_excl} لعدم موثوقية بيانات الوزن" / ""
  q_change_clause: "لم تتغير تقريباً" / "{dir:q:ارتفعت|تراجعت} بنحو {dq_abs:pct0} فقط"
  reason_clause: "لغياب بيانات الوزن" / "لعدم موثوقية بيانات الوزن في أكثر من سنة"
rules: decomposable إذا وُجدت ≥ 3 سنوات بوزن سليم (P1-7)؛ الإشارات المتعاكسة → value_up_price_down أو value_down_price_up؛
       نفس الإشارة وdq ≤ 0.2·dv → price_led؛ وإلا decomposable_*؛ q_share_frac وq_growth_frac وspan من silk_study_numbers.
اختبار الاتجاه المعكوس: (dv=+30%, dp=−10%) → value_up_price_down بلا جملة «تفسّر نحو … ويعود الباقي»؛ (dv=−20%, dp=+8%) → value_down_price_up؛
                       (dv=+43%, dp=+15%) → نص المرجع حرفياً.
```

## ج. قواعد العربية في المحرك (`silk_study_render.py`؛ بنود المالك ٩–١٢)
- حروف الجر مع «ال»: `prep("ل", "السوق") → "للسوق"`، `prep("ب", "الدولار") → "بالدولار"`، `prep("ك", "المرجع") → "كالمرجع"`؛ بلا «ال»: «لـ» + اسم علم أجنبي بشرطة الوصل («لـ{product_short}»). اختبار لكل حرف مع/بلا «ال» ومع اسم أجنبي.
- مولّد العدد والمعدود `count(n, noun)`: 1 → المفرد («دولة واحدة»)، 2 → المثنى («دولتان»)، 3–10 → العدد بعكس جنس المعدود + جمع مجرور («ثلاث دول»، «أربع سنوات»، «ثلاثة موردين»، «ستة أشهر»)، 11–99 → العدد + مفرد منصوب («48 دولة»، «15 مورداً»)، 100 فما فوق → مفرد مجرور («100 دولة»). جدول المعدودات: دولة/دول (م)، سنة/سنوات (م)، مورّد/موردين (ذ)، شهر/أشهر (ذ)، جهة/جهات (م). اختبار لكل فئة بالمذكر والمؤنث.
- مواصفات التنسيق: أرقام لاتينية؛ فاصلة آلاف «1,500»؛ النسبة ملتصقة «19%»؛ «مليون دولار» بعد الرقم برقم عشري واحد «89.4 مليون دولار»؛ الدولار للكيلوغرام «6.2 دولار للكيلوغرام»؛ أسماء الأشهر (يناير…ديسمبر)؛ الفاصلة العربية «،» والفاصلة المنقوطة «؛»؛ الشرطة بين السنوات «2023–2025» (en dash).
- اختبار: لا فعل اتجاه (ارتفع/تراجع/نما/انخفض/زاد/انكمش ومشتقاتها) مكتوباً صلباً خارج `{dir:...}` في أي قالب من `data/study_templates_ar.yaml`.

## د. البيانات والمعرفة (بنود المالك ١٣–١٥)
- كل بند في `data/product_knowledge/*.yaml` يحمل `verified_at`؛ ما تجاوز ستة أشهر يُوسم تلقائياً في المخرج «(آخر تحقق {date}؛ يُراجع)» كما في جدول المتطلبات.
- أولوية التعارض: الأرقام من البيانات الحية دائماً (ملف المعرفة لا يحمل أرقام قرار)؛ النصوص النوعية من ملف المعرفة المعتمد؛ الجهات تُدمج من الملف والبعثات مع تاريخ كل منها ومصدرها؛ كل تعارض يُسجَّل في `silk_ops_log` بحالة `review` ويُدرج في ملف المراجعة.
- «قريبة جغرافياً»: من `data/geodist_l1.csv` (CEPII GeoDist، distcap، عتبة 3,000 كم موثقة في rules)؛ نص المرجع لا يُمس. الملف ينتظر فتح `www.cepii.fr` في سياسة الشبكة أو تزويد المالك به.

## هـ. معيار القبول لـP0-T (بند المالك ١٦)
الاختبارات الثلاثة شروط قبول متساوية الوزن: المطابقة الحرفية لماليزيا؛ الحالة التركيبية (هابطة، HHI مركّز، إرجاء) تمر بالـlinter بالنسخ الصحيحة؛ حالة بلا ملف معرفة بنص حتمي ≥ 70%. فشل أي منها = P0-T غير مقبول.

## الدفعة ١ — الحرجة (تسليم مضمون، جاهزية قبل الخصم، قرار غير مفروض، سلامة الأرقام) — 12 بنداً

P1-1 فحص الجاهزية قبل الحجز (F-03, F-04). ملفات: `api.py` (`_research_impl_after_admission`)، وحدة جديدة `silk_study_readiness.py`، `web/index.html`. التغيير: قبل `try_reserve_usd` تُجلب ثلاث نقاط بمصادر مجانية/مكاشة (سلسلة واردات ≥3 سنوات، حصص موردين، تعرفة) بمهلة قصيرة؛ النقص يُعاد 409 `insufficient_data_preflight` بقائمة الناقص وخيار `accept_limited=true`؛ الواجهة تعرض الخيارين. القبول: لا يُحجز دولار ولا يُستدعى LLM قبل اجتياز الفحص. الاختبار: TestClient مع شبكة مقطوعة → 409 و`usd_spent_today()==0` و`_call_tools` غير مستدعى.
P1-2 سقف تكلفة لكل تقرير إلزامي (F-04, F-30). ملفات: `silk_research_pipeline.py` (`_budget_ok`)، `.env.example`. التغيير: `SILK_RESEARCH_MAX_USD` بقيمة افتراضية (مقترح 4.0) لا «معطّل»؛ فصل `cost_usd_by_stage` (missions/analyst/writer/reviewer/export). القبول: تشغيلة تتجاوز السقف تتوقف قبل النداء التالي وتُسلَّم. الاختبار: نداءات مزيفة بتكلفة مرتفعة → `halted_before` مضبوط والتسليم تام.
P1-3 التسليم المضمون بعد الجاهزية (F-01, F-02, F-05, F-06؛ تعديل المالك ٣). ملفات: `silk_export_gate.py`، `api.py` (docx/pdf/md)، `silk_reports.py`، `web/index.html`. التغيير: نتيجة البوابة FAIL لا تحجب، وتُصنَّف ملاحظاتها صنفين: (أ) فشل ناتج عن **فجوة بيانات** (`source_coverage_below_threshold`، `agent_failed*`، `analyst_layer_failed`…) → يُحوَّل إلى بند في «ما لم يتسنّ توثيقه» مع الجهة القادرة على استكماله؛ (ب) فشل **محتوى** (رقم بلا سند، تناقض، نص تشغيلي، عنصر نائب) → يُعاد توليد فراغه **مرة واحدة** عبر P1-12 (إن كان في فراغ نموذج؛ وإن كان في فقرة (أ) فهو خطأ قالب/بيانات يُسجَّل في `silk_ops_log` للمطوّر ويُحذف الادعاء)، وإن بقي يُحذف الادعاء المعيب ويُدوَّن بنداً في الحدود. خطأ البوابة نفسه = تحذير لا FAIL؛ md يمر بنفس المسار؛ فشل PDF → إعادة محاولة واحدة ثم docx مع ترويسة `X-Silk-Fallback: docx` وتنبيه داخلي `silk_ops_log`. يبقى `?internal=1` للمدقق. القبول: لا مسار 409/501/503 على تصدير تقرير اجتاز الجاهزية. الاختبار: محاكاة كل صف من جدول §٢ (فشل مراجع، انقطاع مزوّد، نفاد ميزانية، فشل soffice، FAIL البوابة بالصنفين) → 200 وملف غير فارغ.
P1-4 مهلة كلية للتشغيلة (F-07؛ تعديل المالك ٥). ملفات: `api.py` (`_research_background`)، `silk_research_pipeline.py`. التغيير: `SILK_RESEARCH_MAX_MINUTES` (افتراضي **35**، يُعدَّل بعد القياس الحي) — عند البلوغ تُوقَف المراحل الباقية ويُسلَّم أفضل إصدار. القبول/الاختبار: خيط طويل مزيف → تسليم خلال المهلة.
P1-5 قالب قرار رباعي حتمي (F-19). ملفات: `silk_synthesis.py`، `silk_study_numbers.py`، `silk_ai_judge.py`. التغيير: خريطة الحكم الحالي → {دخول، دخول مشروط، إرجاء، عدم دخول} مع المتطلبات المرقّمة المشتقة من الفجوات؛ حذف الفقرة النموذجية المائلة وكتلة الإزاحة المتناقضة من البرومبت. القبول: القرار في المخرج = القرار الممرَّر؛ لا نص قرار في العقد. الاختبار: أربع حالات تركيبية تعطي الأربعة.
P1-6 كل الأرقام حتمية (F-14, F-15). ملفات: `silk_study_numbers.py` (جديد)، `silk_ai_judge.py`، `silk_evals.py`. التغيير: تُحسب: سعر الوحدة، CAGR بسنة أساس معلنة، تغيّر الصرف ونطاقه بتعريفه، عدّ السنوات، الحصص، الكسور اللفظية العربية (نصف/ثلث/ربع/سدس/ثلاثة أخماس/نحو نصف/يقارب الربع) بصيغتها الجاهزة؛ حذف كل تعليمة حساب من البرومبت؛ إزالة حصة الإزاحة وإيرادها؛ `_known_numbers` يضيف محوّل كسور عربية وأرقاماً هندية. القبول: `citation_correctness_score==1.0` على المخرج المحاكى بكاتب يُدخل كسراً غير مسند → يُرفض. الاختبار: وحدة لكل صيغة + اختبار AST يمنع أفعال الحساب في البرومبت.
P1-7 تفكيك القيمة إلى كمية وسعر (F-09, F-13). ملفات: `silk_study_numbers.py`، `silk_llm_runtime.py` (:192–219 يضيف `data_year`)، `silk_deep_pillars.py`. التغيير: لكل سنة قيمة/وزن/سعر وحدة؛ استبعاد سنة وزنها يخرج عن نطاق ±60% من وسيط الجوار؛ نسب النمو الثلاث للنافذة السليمة؛ عند التعذر جملة «لا يمكن فصل أثر الكمية عن أثر السعر» مع السبب؛ ملاحظة الإنتاج المحلي عند وجود مؤشر عليه. القبول: سلسلة المرجع (52.4→89.4، 5.4→6.2) تعطي «قرابة ثلاثة أخماس» للكمية. الاختبار: وحدة بأرقام المرجع.
P1-8 وسم «أولي» حتمي (F-13). ملفات: `silk_data_layer.py` (`DataPoint.status` يضيف `provisional`)، `silk_llm_runtime._recent_years`، `silk_reports.py`. التغيير: آخر سنة يُلتقط اكتمالها من كومتريد (`isReported`/تاريخ النشر أو قاعدة ≥ 12 شهراً منذ نهاية السنة)؛ كل رقم أولي يحمل الوسم ولا يُخلط في جملة مع مكتمل (linter). القبول: 2025 في المرجع موسومة أولية. الاختبار: وحدة + linter.
P1-9 HHI متسق (F-10؛ تعديل المالك ١). ملفات: `silk_study_numbers.py`، `silk_market_ranker.py`، `silk_quality_gate.py`. التغيير: HHI من نفس سنة الحصص؛ فحص الاتساق مع الحصص المعروضة: الحد الأدنى `Σs_i²`؛ الحد الأعلى يوزّع الحصة الباقية `r = 100 − Σs_i` على موردين لا تتجاوز حصة أيٍّ منهم أصغر حصة معروضة `s_min`: `upper = Σs_i² + floor(r/s_min)·s_min² + (r mod s_min)²`. لحصص المرجع 19/16/14/8: `Σs²=877`، `r=43`، `s_min=8` → `877 + 5·64 + 3² = 1,206`. إزالة الصيغة 0–1 من الرانكر لصالح القانونية. القبول: 900–1,200 تمر، **1,300 تُرفض**. الاختبار: وحدة بالحالتين + حالة حدّية (`r < s_min`).
P1-10 إصلاح انهيار المنافسين (F-08). ملفات: `silk_llm_runtime.py:256–283`. التغيير: تجاهل نقاط `value=None` قبل اختبار المرآة والحساب. القبول: فشل الجلب → مرآة تُجرَّب ثم فجوة موسومة `fetch_failed`. الاختبار: يقفل الرسالة «'NoneType' object has no attribute 'get'».
P1-11 بوابة HS تقبل الصفات الوصفية غير المغيِّرة للتصنيف (F-31؛ تعديل المالك ٢). ملفات: `silk_hs_confirm.py` (`preflight_block`/قياس التداخل)، `data/hs_codes.csv` (مرادفات 090121 تشمل «غير منزوعة الكافيين»/«not decaf»). التغيير: حين يطابق الرمز الاسم الأساسي، لا تُحتسب الصفات التي يذكرها وصف الرمز الرسمي نفسه (not decaf، roasted) صفاتٍ غائبة. القبول: «قهوة محمصة غير منزوعة الكافيين» ↔ 090121 يمر بلا 422؛ «زبدة الفول السوداني» ↔ 040510 ما زال يُرفض. الاختبار: حالة القهوة + حالة الانحدار الأصلية.
P1-12 إعادة توليد الفراغ المعيب (تعديل المالك ٣؛ الصيغة الوحيدة). ملفات: `silk_study_render.py`، `silk_study_linter.py` (الحد الأدنى: عنصر نائب، رقم بلا سند، مخالفة معجم). التغيير: ينطبق على فراغات (ب)/(ج) فقط؛ فراغ يفشل الفحص الحتمي يُعاد **مرة واحدة** بنفس المدخلات + الملاحظة؛ الفشل الثاني = حذف الادعاء وبند في «ما لم يتسنّ توثيقه». فقرات (أ) والأرقام والجداول لا تُمس. القبول: نموذج مزيف يعيد النائب دائماً → التسليم بدونه وبنداء إضافي واحد لكل فراغ. الاختبار: عدّ النداءات.

## الدفعة ٢ — الأسلوب — 7 بنود

P2-1 أقسام النمط `study` الأحد عشر من ملف القوالب ومحرك التعبئة (F-23). ملفات: `data/study_templates_ar.yaml` (العناوين الحرفية والترتيب)، `silk_study_render.py`، `silk_reports.py` (تصدير النمط بلا دمج)، `api.py` (`?style=study`)، `web/index.html`. لا تعديل على `_REPORT_SECTIONS` (نمط decision يبقى). القبول: docx/pdf/md بالعناوين الأحد عشر والتسميات العريضة حرفياً. الاختبار: هرمتي على المصدّرات الثلاثة (docx عبر python-docx، pdf عبر pdftotext).
P2-2 الترويسة الحتمية (F-23). ملفات: `silk_reports.py`. التغيير: منتج+HS، منشأ، سوق، تاريخ الإعداد، أحدث سنة مكتملة (من P1-8). الاختبار: مقارنة نصية.
P2-3 نماذج لفراغات (ب)/(ج) فقط (F-22). ملفات: `data/study_style_exemplars.json` (لكل فراغ: الجملة المرجعية المقابلة وصيغ المعجم المسموحة)، `silk_study_render.py` (تمريرها مع تعليمة «جملة أو جملتان، بصيغ المعجم فقط، دون أي رقم لم يُمرَّر»). لا نماذج لأقسام كاملة. القبول: كل فراغ يستلم نموذجه. الاختبار: التقاط البرومبت.
P2-4 عقد `STUDY_SLOT_CONTRACT` لفراغات (ب)/(ج) فقط (F-22, F-21). ملفات: `silk_style_contract.py`. التغيير: عقد قصير للفراغ: صوت التقرير، صيغ المعجم الإلزامية، المحظورات (ضمير المتكلم، صفات تسويقية، تعجب، إنجليزية عدا الأعلام والرموز، عبارات بلا رقم أو جهة)، منع أي رقم غير ممرَّر؛ لا عقد كاتب كامل بعد الآن. حذف القواعد الخاصة بمنتج من البرومبت والعقد العام إلى ملفات المعرفة. القبول: اختبار AST يمنع أسماء منتجات/أسواق في العقد.
P2-5 الجداول الخمسة بأعمدتها (F-23, F-24). ملفات: `silk_study_numbers.py` (بناء الجداول حتمياً)، `silk_reports.py` (عرضها في docx/md/pdf). سلسلة الواردات + سطر «المصدر:»؛ أسعار الرف (المنتج، التصنيف، السعر للكيلوغرام، المصدر)؛ المتطلبات (المتطلب، الجهة المختصة، الطرف المسؤول، آخر تحقق)؛ الجهات (الجهة، الوصف، الدور المقترح)؛ الخطة (الفترة، الإجراء، المتطلب المرتبط، الجهة المنفذة). الكاتب لا يكتب الجداول. القبول: الأعمدة مطابقة حرفياً. الاختبار: مخطط أعمدة.
P2-6 linter أسلوبي حتمي (F-24). ملفات: `silk_study_linter.py` (جديد)، `silk_quality_gate.py` (يُستدعى قبل التسليم). يرفض: تعداداً خارج الجداول، محظوراً، قسماً بلا صيغة من المعجم، جدولاً ناقصاً/بأعمدة مختلفة، عنواناً غير حرفي، خلط أولي بمكتمل، مصطلحاً خارج القاموس. القبول: المرجع يمر بصفر مخالفات؛ الشاهد الكويتي يُرفض. الاختبار: الملفان.
P2-7 حل تعارض HHI/«مؤشر تركّز» (F-17). ملفات: `silk_style_contract.py`، `silk_reports.py`، `silk_quality_gate.py`. الصيغة الموحدة كما في المرجع: «مؤشر تركّز الموردين». الاختبار: linter.

## الدفعة ٣ — الاتساق والمنطق — 8 بنود

P3-1 سجل ادعاءات (F-16). ملفات: `silk_study_claims.py` (جديد). كل ادعاء بحالة {موثق+مصدر، يُفاد، فجوة، قاعدة تقديرية، توقع}؛ يُمرَّر للكاتب ويفحص linter تعارض الحالة بين الأقسام والجداول. الاختبار: نص بادعاء مقرَّر في قسم ومتحفظ في آخر → رفض.
P3-2 قاموس مصطلحات الحالة (F-16, F-17). ملفات: `silk_study_claims.py`، `silk_study_linter.py`. الشرائح وتسميات النطاقات السعرية. الاختبار: مصطلح مغاير → رفض.
P3-3 قائمة «ما لم يتسنّ توثيقه» + المصادر + الجملة الختامية حتمياً (F-33). ملفات: `silk_reports.py` (`_client_gaps_section`، `_client_references_section`). من فجوات DataPoints + ادعاءات «يُفاد»، مع الجهة القادرة على الاستكمال؛ المصادر من أرقام المتن فعلاً؛ الختام من التغطية الفعلية. الاختبار: مصدر في المتن غائب عن القائمة → رفض؛ فجوة بلا جهة → رفض.
P3-4 ملف المصدّر (F-20). ملفات: `silk_economics.py` (`product_card.exporter_type`)، `api.py`، `web/index.html`، `silk_ai_judge.py`، المراجع. القيم: منتج زراعي/محوِّل لمادة مستوردة/مصنّع؛ كل استنتاج ينسب نفعاً للمصدّر يُفحص عليه في المراجعة. الاختبار: حالة «محوِّل» + ادعاء «يفيد المصدّر توجه الشراء المباشر من الدول المزارعة» → ملاحظة حرجة.
P3-5 الميزة التنافسية مشروطة بالجدوى (F-20). ملفات: العقد + معيار المراجع. كل ميزة تقترن بشرط (كميات، تكلفة، أثر التصنيف) أو توسم «لا يُعتد بها قبل التحقق». الاختبار: linter.
P3-6 قسم الحجة المضادة وشروط انقلابها + مؤشرات إعادة النظر (F-19). ملفات: `silk_study_numbers.py` (أقوى دليل مضاد يُحدَّد حتمياً: أكبر حصة مورد، غياب السعودية، مركز إعادة تصدير)، العقد. الاختبار: القسم يحمل شرط انقلاب واحداً على الأقل.
P3-7 سعر الحدود متوسط عام لا مرجع تسعير (F-12). ملفات: `silk_research.py` (SOM لا يعتمد على سعر الحدود لمنتج متمايز: يُعلن «يتطلب سعر رف الشريحة»)، `silk_economics.py`، البرومبت. الاختبار: نص يقدّمه مرجعاً تفاوضياً → رفض.
P3-8 مرجع الرف بالشريحة وعتبة مؤقتة (F-11). ملفات: `silk_economics._pick_shelf_anchor`، `silk_study_numbers.py`. الشريحة من `product_card.tier`؛ بلا مرجع من الشريحة: العتبة توسم «حداً مؤقتاً» ويُمنع الإرجاء عليها؛ قاعدة 40–45% موسومة تقديرية. الاختبار: حالة المرجع تعطي 15$ حداً مؤقتاً.

## الدفعة ٤ — المعلومات الميدانية — 4 بنود

P4-1 استخراج جهات مهيكل (F-27). ملفات: `silk_channels_agent.py`، `silk_competitors_agent.py`، `silk_missions.py` (بعثة القنوات)، مخطط `entity{name, type, role∈{مرشح، منافس، شريك محتمل، قناة رصد}, evidence_url, date, product_confirmed}`. الاستبعاد المعلل لغير المثبت تعامله بالمنتج. الاختبار: نتائج بحث مزيفة → 3 جهات مصنفة + سطر استبعاد.
P4-2 أسعار رف بالشريحة (F-11, F-27). ملفات: `silk_missions.py` (`pricing_scout`)، `silk_product_pages.py`. حقل `segment∈{مختص، جماهيري، تقليدي، غير مكافئ}` لكل سعر مع المصدر والتاريخ. الاختبار: جدول الأسعار بعمود التصنيف.
P4-3 شروط المنافذ (F-27). ملفات: بعثة القنوات. ادعاءات «يُفاد بأن [المنفذ] يشترط…» تدخل سجل الادعاءات بحالة «يُفاد» تلقائياً. الاختبار: السجل.
P4-4 حد أدنى جهتان–ثلاث أو فجوة بجهة الاستكمال (F-27). ملفات: `silk_study_numbers.py`، العقد. الاختبار: <2 → بند فجوة بمصدر محدد (دليل JAKIM/قوائم معارض السوق من `data/`).

## الدفعة ٥ — حلقة المراجعة وسقف التكلفة — 6 بنود

P5-1 توسيع فحوص الفراغات (يبني على P1-12؛ F-18, F-24, F-29). ملفات: `silk_study_linter.py` (المعجم، المحظورات، الاتساق، المصطلحات). الحد يرتفع إلى محاولتين للفراغ داخل الحلقة؛ ثم حذف الادعاء وبند حدود. الاختبار: كما P1-12 مع الفحوص الموسّعة.
P5-2 المراجع المستقل بمعيار مكتوب (F-29؛ مراجعة المالك أ-٣). ملفات: `docs/STUDY_REVIEW_RUBRIC.md` (جديد)، `silk_ai_judge.review_report` (نموذج/إعداد مختلف). يُخرج درجة /10 وملاحظات {موضع، شدة، نص تصحيح} تغطي البند 8 والمنطق والاتساق والأسلوب. ملاحظاته على فقرات (أ) لا تدخل حلقة إعادة الكتابة: تُسجَّل في `silk_ops_log` بصنف `template_error` أو `data_error` للمطوّر. الاختبار: مخرج JSON بالمخطط + ملاحظة على فقرة (أ) → سجل لا إعادة كتابة.
P5-3 حلقة محدودة (F-29). جولتان كحد أقصى؛ إعادة ملء الفراغات (ب)/(ج) ذات الملاحظات الحرجة/العالية فقط؛ فقرات (أ) والأرقام والجداول لا تُمس؛ إعادة الفحوص الحتمية بعد كل جولة؛ توقف عند ≥9؛ لا إعادة جمع. الاختبار: مراجع مزيف يعطي 9.2 → جولة واحدة.
P5-4 اختيار أفضل إصدار (F-29). حفظ إصدارات الجولات مع درجاتها؛ التسليم للأعلى درجة. الاختبار: الجولة 2 أدنى → تسليم الجولة 1.
P5-5 سقف الكتابة+المراجعة وتسجيل الاقتصاديات (F-30). `SILK_STUDY_TAIL_MAX_USD`؛ عند البلوغ توقف وتسليم؛ تسجيل التكلفة بالمرحلة والزمن والدرجة في `economics` ولوحة داخلية (`GET /ops/studies`). الاختبار: بلوغ السقف → تسليم + سجل.
P5-6 سلّم التسليم بالدرجة (F-01). ≥9 عادي؛ 8–9 عادي + تحويل الملاحظات المتوسطة إلى حدود؛ <8 عادي + تنبيه مراجعة بشرية + علم «إصدار محدَّث مجاني». الاختبار: الثلاث حالات.

## الدفعة ٦ — الحراسة والتعميم — 7 بنود

P6-1 رفض العناصر النائبة في `_client_assert_clean` (F-18): `[`…`]`، «يُدرج»، TODO، placeholder، `<…>`؛ وتنظيف قوالب البرومبت. الاختبار: كل نمط.
P6-2 المؤشرات العامة إلزامية بإعادة محاولة (F-26, F-25): WGI/LPI/FX 3 محاولات بتراجع؛ الفشل يُسجَّل `silk_ops_log` خطأ خط بيانات لا فجوة صامتة؛ تغليف WGI يحفظ `data_year/status/unit/url`. الاختبار: فشل ثم نجاح → قيمة؛ الفشل الدائم → خطأ مسجّل.
P6-3 جدول المتطلبات بـ«آخر تحقق» (F-28): من `verified_at`؛ >6 أشهر يُوسم؛ غير المؤكد «يُستوضح»؛ الضرائب غير المحددة «يُحدَّد المعدل من جدول [الجهة]». الاختبار: صف قديم → وسم.
P6-4 جدول مراكز إعادة التصدير (F-32): `data/reexport_hubs_l1.csv` مذكور المصدر يوسم الموردين؛ القاعدة الحالية احتياط. الاختبار: سنغافورة موسومة.
P6-5 الحالة الذهبية `evals/golden_set/malaysia_coffee.json` (F-35) بتأكيدات البند 8 التسعة عشر + `samples/golden_malaysia_coffee_study.md`؛ ملف حالة يوسم الأرقام السياقية والقواعد التقديرية (مدة الشحن، 50–60%، تاريخ تأسيس المحامص). الاختبار: `tools/golden_set.py compare`.
P6-6 حالتان ذهبيتان مختلفتان جذرياً (منع فرط الملاءمة): منتج غير غذائي في سوق بلا حلال (مثلاً بوليمرات→تركيا من `tools/canonical_turkey_polymers.py`)، وحالة إرجاء/عدم دخول. الاختبار: >8.5 بكاتب حي.
P6-7 التوثيق: درس في `docs/LESSONS.md` + مرساة `tests/test_lessons_enforcement.py`؛ قرار في `docs/DEEP_RESEARCH_DECISIONS.md`؛ إعادة توليد `samples/`؛ اختبار PDF يتخطى عند محرك معطوب (F-34).

## بنود مطبَّقة فعلاً (لا تُعاد)
- حجز دولاري قبل التشغيل وتسوية عند الفشل (`api.py:2936`، `silk_storage.py:519/543`).
- سقف يومي بالدولار ونداءات (`silk_usage`، `pipeline:75–76`).
- تسجيل `cost_usd_by_mission`/`stage_seconds`/تكلفة حية في status.
- فحص placeholder جزئي حاجز (`placeholder_leak`, `client_section_placeholder`, `client_scaffold_leak`).
- `gaps_closing_contradiction` حاجز للجملة الختامية.
- HHI بدالة قانونية واحدة (`silk_economics.hhi`) في المسار الرئيسي.
- `verified_at` موجود في CSV والصفوف الماليزية الستة محدثة 2026-09-23.
- بوابة HS المسبقة وقفل الحكم من `synthesize`.
- الإيقاف المبكر (يوفر المحلل والكاتب، لا البعثات).

## تقدير التكلفة والزمن لكل تقرير (تقدير P0-T وحده؛ يُستبدل بالقياس الحي)
- خط الأساس الحالي يُقاس في الخطوة ٠ (لا تقدير من الوثائق يُعتمد).
- بعد P0-T: البعثات كما هي + نموذج لفراغات (ب)/(ج) الناقصة فقط (≤ 8 فراغات قصيرة) + مراجع للفراغات؛ التقدير الأولي ≈ 1.4–1.8$ وزمن أقل من الحالي بنحو 5–8 دقائق، ويُثبَّت بالقياس بعد النسخة الأولى القابلة للعرض ثم بعد كل دفعة (`docs/audit/STUDY_MODE_RUNS.md`).

## ما لا يُضمن
- حالة ماليزيا بملف معرفتها المعتمد تطابق المرجع حرفياً (اختبار P0-T-1). المنتجات والأسواق الأخرى تطابق لغة المرجع حرفياً في الفقرات الحتمية (نحو 75% من النص) وتقاربها في فقرات النموذج (ب/ج) الموسومة للمراجعة.
- المعلومات الميدانية تظهر فقط إن التقطتها مهام الجمع أو ملف معرفة معتمد، وإلا تُعلن فجوة بجهة استكمالها.
- أول 10 دراسات بعد التنفيذ تُراجع بشرياً بعد تسليمها (لا قبله)، وتُضاف الأخطاء الجديدة إلى البند 8 والمعيار، والنسخ البديلة الجديدة إلى ملف المراجعة.
- درجة خط الأساس للكاتب الحي الحالي تبقى «no sufficient evidence» حتى الخطوة ٠.

## التحقق النهائي
رتب ١–٤ كما في الطلب؛ تشغيل حي للحالات الذهبية الثلاث بالحلقة مع تسجيل الدرجة/التكلفة/الزمن؛ اختبارات التسليم والتكلفة (كل مسار §٢ يسلّم ملفاً؛ لا خصم قبل الجاهزية؛ لا تجاوز للسقف)؛ حَكَم مستقل يقارن ماليزيا بالمرجع ≥9؛ `/code-review` قبل PR؛ الدفع إلى `claude/elegant-goodall-gnr2lp`.

## تسلسل ما بعد الموافقة (بلا كود منتج)
1. حفظ الملفات الثلاثة: `samples/golden_malaysia_coffee_study.md` (النص المرجعي حرفياً، محفوظ مؤقتاً في scratchpad)، `docs/audit/STUDY_MODE_AUDIT.md` (الجزء الأول)، `docs/plans/STUDY_MODE_FIX_PLAN.md` (الجزء الثاني بتعديلات المالك ٠–٧).
2. الخطوة ٠: التشغيل الحي ضد النشر — المالك يضع `SILK_API_BASE` و`SILK_API_KEY` في بيئة الجلسة بنفسه (لا قراءة من Railway)؛ لا يُطبع المفتاح في أي ملف أو رد؛ ثم `tools/acceptance_run.py` وتسجيل خط الأساس الرسمي.
3. التوقف وعرض النتيجة و`docs/plans/STUDY_TEMPLATE_VARIANTS_REVIEW.md` قبل الدفعة ١.
