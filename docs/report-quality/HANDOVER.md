# التسليم — صفحةٌ واحدة · handover

**الفرع:** `claude/jolly-bell-q5v8ia` · **الحزمة:** `5193 passed / 63 skipped
/ 0 failed` (هرمتيّ) · **خطّا الأساس:** ١٦ مدوّنةً مطابقةً حرفياً بالرايات
مطفأة، و`golden_set compare` OK.

## ما الذي تغيّر، بجملةٍ لكلّ صنف

| # | العيبُ المرصود | موطنُ الفكس | الحارس |
|---|---|---|---|
| ١ | لغةُ نظامٍ تصل القارئ | `READER_LANGUAGE_RULE` + إعادةُ صياغةِ ثلاثِ قوالبَ حقيقية | `reader_language_leak` |
| ٢ | خانةٌ فارغةٌ تكسر الجملة (٦٣ قالباً من ٨٦) | صيغةُ فراغٍ نحوية `<key>_empty` + مُصلِحٌ حتميّ | `template_interpolation` |
| ٣ | خمسُ عائلاتِ تنسيقٍ متوازية | `silk_narrative` مُنسِّقاً واحداً يقرأ منه كلُّ مُصدِّر | `score_format_drift` وأخواتها |
| ٤ | جهةٌ واحدةٌ بتسميتين، ونشاطٌ إنجليزيٌّ في جدولٍ عربيّ | جدولُ ترجمةٍ عند حدّ العرض + تعريفٌ حين يَرِد المصطلح | `authority_naming_drift` |
| ٥ | حقيقةٌ واحدةٌ مشروحةٌ في خمسة أقسام | قاعدةُ «قسمٌ واحدٌ يشرح» + تنويعُ الروابط | `cross_section_near_duplicate` |
| ٦ | قيمتان لمؤشرٍ واحد | `silk_figure_store` — معرّفٌ ثابتٌ لكلّ قراءة | `metric_value_divergence` (حاجبٌ بالراية) |
| ٧ | ثلاثةُ أعدادٍ لقائمةِ شروطٍ واحدة | `open_conditions` مُعِدّاً واحداً | `open_conditions_count_mismatch` (حاجبٌ بالراية) |
| ٨ | «ثقةٌ عالية» وعمودٌ أساسيٌّ مجهول | سقفُ التسمية + حسابٌ يُعيد إنتاج الدرجة | `high_confidence_with_missing_pillar` (حاجبٌ بالراية) |
| ٩ | رقمٌ مشتقٌّ بلا معادلةٍ ولا عملة | `fmt_derived` — مدخلاتٌ بمصادرها وناقصٌ مسمّى | `reference_to_nonexistent_figure` (حاجبٌ بالراية) |
| ١٠ | افتراضاتُ بنية السوق (صرفٌ/سلطة/بندٌ واسع/روابط/أنظمة) | `silk_market_structure` + مخطَّطٌ مُدقَّق + سجلُّ أنظمة | ستّةُ حرّاسَ تحذيرية |
| ١١ | فحصٌ حاجبٌ يُطلِق على الصحيح | تضييقُ `evidence_body_numeric_contradiction` + عتبةٌ بفصلٍ مقيس | مدوّنةُ مصر تثبيتةً موجَبة |
| ١٢ | «غير متاح» وهو مرصود (تعريفةٌ وعملة) | مفرداتُ تعرّفٍ من مصدرٍ واحد | `observed_value_declared_unavailable` |
| ١٣ | «2539350 INR (المدى 2539350–2539350، ±0%)» | `canonical_decision_value` | `decision_number_format_drift` |
| ١٤ | حارسٌ يُضخِّم عدَّه بنفسه («منفذ الدخول» بوّابةٌ اسمُها «الدخول») | `_named_gateways` + استبعادٌ بالاسم | نفسُ حارسِ الإقليم بعد تضييقه |
| ١٥ | «ميناء طرابلس أو ميناء بنغازي» تُعَدّ صدىً | الرؤوسُ العامّة في استثناء الوحدات | نفسُ فحصِ الصنف ٢ بعد تضييقه |
| ١٦ | تحذيرٌ إلزاميٌّ لفَّه سطرٌ جديد **فحُجِب تقريرٌ سليم** | `_flat_ar` لمطابقةِ الحضور الحرفيّ | نفسُ الفحصِ الحاجب بعد إصلاحه |
| ١٧ | حارسٌ بفرعٍ مُهيَّأٍ وحدَه **لا يُطلِق أبداً** (والاختبارُ يؤكّد سكونَه) | فرعٌ مرصودٌ بلا تهيئة: سعرُ حدودٍ يفوق سعرَ الرفّ ⇒ انقلابُ سلسلةِ قيمة، بعتبةٍ مقيسةٍ ١.٢٥ | `border_price_out_of_range` صار حيّاً |
| ١٨ | «تسجيل REACH» و«علامة CE» تُحقَن بحسب فصلِ البند لا بحسب السوق | `_HS_CATEGORY_SCOPED` — تقييدٌ بسوق الهدف بلا تفريعٍ قُطريّ | `regime_not_belonging_to_country` (قائمٌ، يقرأ المتن) |
| ١٩ | حدٌّ مُعلَنٌ **أوسعَ من قياسه** («ريال» تُوسَم SAR في أسعار الرفّ) | تصحيحُ الحدّ إلى المقيس + لِنتُ مصدرٍ يقفل نطاقَ عكسِ العملة | `test_c19_...scoped_to_the_exporters_own_currency` |

## جدولُ الرايات — كلُّها مطفأةٌ افتراضياً

| الراية | الصنف | عند التفعيل |
|---|---|---|
| `SILK_FIGURE_STORE` | ٦ | مخزنُ الأرقام بالمعرّف + `metric_value_divergence` حاجب |
| `SILK_OPEN_CONDITIONS_SINGLE` | ٧ | قائمةُ شروطٍ واحدة + `open_conditions_count_mismatch` حاجب |
| `SILK_CONFIDENCE_DISCIPLINE` | ٨ | سقفُ نطاقِ الثقة + عرضُ الحساب + الحاجب |
| `SILK_DERIVED_PROVENANCE` | ٩ | معادلةٌ ومدخلاتٌ بمصادرها + رمزُ ISO + الحاجب |
| `SILK_RECOGNITION_VOCABULARY` | ١٢ | **تصحيحٌ رقميّ**: تعريفةٌ تُقرَأ بدل ٠٪، وعملةٌ تُقرَأ فيُحسَب قسمُ الاقتصاد |
| `SILK_DECISION_NUMBER_FORMAT` | ١٣ | فاصلُ آلافٍ في أرقام القرار، ولا مدىً منحلّ |
| `SILK_MARKET_STRUCTURE_CONFIG` | ١٠ | تعليمةُ بعثةٍ مقيَّدةٌ بسوق الهدف، ومِصفاةُ نشاطٍ وعمودُ سبب، وإفصاحُ اتّساعِ البند، ومعنى صفرِ التقلّب |

| `SILK_MARKET_STRUCTURE_CONFIG` | ١٨ | تركيزُ الاشتراطات مقيَّدٌ بسوق الهدف بلا اسمِ نظامِ كتلةٍ أجنبية |

الأصناف ١–٥ و١١ و١٤–١٧ و١٩ **بلا راية**: تحذيريةٌ أو تضييقُ فحصٍ قائمٍ أو
تصحيحُ حدٍّ مُعلَن، والسلوكُ السابق هو نفسُه حين لا تُطلِق.

**التوصية:** تُفعَّل `SILK_RECOGNITION_VOCABULARY` أوّلاً — هي الوحيدة التي
تُصلِح رقماً خاطئاً لا تُضيف إفصاحاً.

## كيف يقرأ المالكُ خرجَ البوابة بنفسه

```bash
# حكمُ البوابة وملاحظاتُها على كلّ مدوّنةٍ مجمّدة (١٦)
python3 -c "
import importlib, silk_render, silk_quality_gate as G
from tools import gen_verdict_baseline as B
for k,(m,f) in B.CANONICAL_BLOBS.items():
    v=silk_render.build_view(getattr(importlib.import_module(m),f)())
    o=G.run_quality_gate(v)
    print(k, o['verdict'], [x['check'] for x in o['findings']])"

# إثباتُ «القيم مقدّسة» بسكربت لا بالعين
python3 tools/report_quality_baseline.py compare     # ١٦ حالة مطابقة حرفياً
python3 tools/golden_set.py compare
```

## ما لم يُنفَّذ

في `docs/report-quality/LOGIC_ISSUES.md` — أربعةُ مفاتيحِ تهيئةٍ تنتظر
مصدرَ المالك (العقدُ مشحونٌ والصفوفُ فارغة)، وأربعةُ قراراتٍ مؤجَّلة، وثلاثةُ
بنودٍ في نطاقٍ آخر، ومناطقُ العمى المُعلَنة لكلّ حارس.

## الوثائق

`DISCOVERY.md` (الاكتشافُ وما خالف نصَّ المهمة) · `CHANGELOG.md` (صنفٌ صنفاً
بفرقِ الراية المقيس) · `LOGIC_ISSUES.md` (ما لم يُنفَّذ) · وهذه الصفحة.
وسجلُّ الدروس `docs/LESSONS.md` أسطر **٢٣٤–٢٥٢** (تسعةَ عشرَ صنفاً)، كلٌّ
بمرساةٍ في `tests/test_lessons_enforcement.py` وحارسٍ في
`tests/test_regression_registry.py`.
