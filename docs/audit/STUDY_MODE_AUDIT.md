# فحص نمط «دراسة السوق التصديرية» — STUDY_MODE_AUDIT

المرحلة الأولى (قراءة فقط). بيئة الفحص: الفرع `claude/elegant-goodall-gnr2lp` على `d568ec4`، 2026-09-26. الحاوية كانت بلا pytest/fastapi/python-docx/libreoffice-writer/pdftotext/خط Amiri (ثُبّتت للفحص). المرجع: `samples/golden_malaysia_coffee_study.md`.

## ١. خريطة خط التوليد (كل موضع من الكود الحالي)

استقبال الطلب
- `GET /research/readiness` → `api.research_readiness` :2329 ← `_readiness_checks` :2240. لا يحجز شيئاً؛ يفحص HS والتغطية والعقوبات وميزانية كومتريد ومفاتيح الحماية فقط. **لا فحص لكفاية بيانات السوق.**
- `POST /research` → `api.research` :3149 → `_research_impl` :2361 → `_research_impl_after_admission` :2927.
- بوابة تأكيد HS: `silk_hs_confirm.preflight_block` :869 (422 `hs_requires_confirmation`؛ يُتجاوز بـ`hs_confirmed=true`). في التشغيل المرجعي رفضت «قهوة محمصة غير منزوعة الكافيين» ↔ 090121 لأن `data/hs_codes.csv:34` يصف الرمز «بن محمص» فقط.
- مقعد التزامن `silk_research_runtime.try_acquire` :2903 (503 `research_busy`).
- التشغيل: خيط خلفي `_research_background` :2181 (202 + `poll_url`) أو متزامن :3104. **لا مهلة كلية للتشغيلة**؛ مهل النداء الواحد فقط (`silk_ai_judge._TIMEOUT=60` :24، `_LONG_TIMEOUT=300` :29، المراجع 90ث :3229، البثّ 120ث خمول/900ث كلي `silk_llm_provider` :84–92).

الخصم والتكلفة
- حجز دولاري **قبل أي جمع**: `silk_usage.try_reserve_usd(expected_run_usd())` في `api.py:2936`؛ الافتراضي **3.0$** (`silk_usage.expected_run_usd` :106، `SILK_RESEARCH_EXPECTED_USD`). الفشل = 429 `daily_usd_budget_exhausted`.
- التسوية: نهاية سليمة `reconcile_run_usd_final` (`silk_research_pipeline.py:792`، `silk_storage.py:519`)؛ انهيار `reconcile_failed_run_usd` (`silk_storage.py:543`، يُستدعى `api.py:2215/3078/3125`) = يبقى الفعلي المُنفَق ويُحرَّر الباقي.
- حارس داخل التشغيلة `_budget_ok` (`silk_research_pipeline.py:259–289`): `SILK_RESEARCH_MAX_USD` (**غير مضبوط = معطّل**) + السقف اليومي؛ يتخطّى المراحل المدفوعة المتبقية ولا يجهض نداءً جارياً.
- سقف نداءات: `SILK_RESEARCH_MAX_LLM_CALLS=40`، `SILK_RESEARCH_MAX_TOOL_CALLS=100` (`silk_research_pipeline.py:75–76`).
- المنصة (`silk_platform`): `quota.reserve_launch` :163 عدّاد إطلاقات لا مال؛ `billing.charge_metered` :64 **غير مستدعى في أي موضع**؛ الخصم الوحيد من المحفظة هو فوترة التخزين الشهرية (`jobs.run_storage_billing` :51). أي: **لا خصم للعميل لكل تقرير اليوم؛ الرصيد الفعلي المستهلك هو رصيد Anthropic للمشغّل**، ويُحجز بالكامل قبل أول نداء.

جمع البيانات (كلها عبر `silk_llm_runtime` داخل بعثات `silk_missions.run_all_missions` :763 / `deep_research` :951؛ وكلاء `silk_research.py` الحتميون ليسوا على هذا المسار)
- واردات: `_tool_comtrade_imports` :149 ← `silk_data_layer.comtrade_trade` :767. القيمة بـ`data_year`؛ سعر الوحدة (قيمة÷وزن) :192–219 **بلا `data_year`**. الفشل = `fetch_failed`؛ الفراغ = مرآة `mirrored` ثقة 0.6.
- حصص الموردين/HHI: `competition_summary_findings` :238 ← `silk_data_layer_v2.market_competitors` :287. **عيب:** عند فشل الجلب تعيد `[DataPoint(None, status="fetch_failed")]` (:300) — قائمة غير فارغة — فيُتخطّى احتياط المرآة (`if not comps` :256) ثم ينهار `c.value.get("share")` (:283) بـ`AttributeError`؛ الإلحاق الحتمي يبتلعه تحذيراً (`silk_missions.py:754` «competitors structured augment skipped: 'NoneType' object has no attribute 'get'» — ظهر حرفياً في التشغيل المرجعي).
- التعرفة: `silk_tariffs_agent.tariff_with_fallback` :243 (WTO → WITS → فجوة؛ GCC = `documented_agreement`).
- الصرف: `PA.NUS.FCRF` (`silk_llm_runtime.py:71`) + تقلب `_augment_risk_news_fx` (`silk_missions.py:625`)؛ **يتوقف بعد أول فشل حي** (:651).
- IMF: `silk_imf_agent.imf_indicator` :75، أداة اختيارية لا تُلحق تلقائياً (`silk_missions.py:936–945`).
- LPI: `LP.LPI.OVRL.XQ` مع تثبيت سنة الإصدار (`silk_data_layer.py:866–874`).
- WGI: `PV.EST/RQ.EST/RL.EST` تُلحق دائماً ببعثة المخاطر (`silk_missions.py:511–552`) لكن التغليف :540–548 **يُسقط `data_year`/`status`/`unit`/`url`**.
- المتطلبات: `lookup_reference("requirements")` ← `RequirementsAgent` (`silk_requirements_agent.py:416`) من `data/requirements_l1.csv` (أعمدته تشمل `verified_at` و`requirement_status`).
- القنوات/المستوردون/المنافسون بالاسم: وكلاء مجانيون ببحث ويب (`silk_channels_agent.py:31`، `silk_importers_agent.py:26`، `silk_competitors_agent.py:28`) ثقة 0.4 «غير محقق»؛ أسعار الرف: بعثة `pricing_scout` ببحث ويب + قراءة صفحات (LLM). الوكلاء المدفوعون (LocalPrice/Volza/Explee) خارج `/research`.
- التخزين: كاش طلبات ملفي (`silk_cache.cached_get`: كومتريد 30 يوماً للسنوات المغلقة، البنك الدولي 7 أيام)؛ البعثات تقرأ من مخزن الحقائق ولا تكتب فيه (الكتابة في `tools/refresh.py`/`silk_collectors.py`).

بنية DataPoint (`silk_data_layer.py:350–391`): `value, source, confidence, note, retrieved_at, status, data_year, source_ids, unit, url, reference_period, retrieval_method, evidence_ids, raw_evidence`. `status ∈ {"", fetch_failed, no_record, mirrored, documented_agreement}` — **لا حالة «مكتمل/أولي»**. تصنيف الفجوات G1–G4 بمطابقة كلمات في `note` (:421).

الإيقاف المبكر: `silk_research_pipeline.py:337–352` بعد اكتمال **كل** البعثات (المدفوعة) — إن كانت الأعمدة المحسوبة دون الحد الأدنى (3 من 5) يُتخطّى المحلل والكاتب. الافتراضي مفعّل (`api.early_halt_enabled` :464).

الكاتب: `silk_ai_judge.deep_report` :1509؛ الأقسام `_REPORT_SECTIONS` :821 (١١ قسماً: الخلاصة التنفيذية، منهجية البحث ونطاقه، نظرة عامة على السوق وحجمه، ديناميكيات السوق، تحليل المستهلك والطلب، المشهد التنافسي، التنظيم والوصول للسوق، اللوجستيات وسلسلة الإمداد، تقييم المخاطر، التوصيات الاستراتيجية، الملاحق). النمط الافتراضي `decision` (`silk_research_pipeline._default_report_style` :64)، والأكاديمي خيار صريح.
المراجعة بعد الكتابة: `write_reviewed_report` :3340 ← `review_report` :3104 (نموذج سريع، 90ث). `_max_review_cycles` :3265: الافتراضي **1** (مراجعة بلا تنقيح)، السقف 2؛ التنقيح فقط للمشاكل الحاجبة (:3515) وهو نداء كاتب كامل (~+0.4$).
الدمج للعميل: `silk_reports._CLIENT_SECTION_BY_ORDINAL` :3358 (1→الملخص؛ 2،11 تُحذف؛ 3–5→«السوق بالأرقام»؛ 6→«المنافسة والتسعير والهامش»؛ 7–8→«مسار الدخول والمتطلبات»؛ 9→«المخاطر»؛ 10→«القرار وأساسه»).
التنقية: `_client_sanitize` :3052، `_client_redact_residual` :3291، `_client_assert_clean` :3305 (+`data/client_forbidden_terms_l1.csv`). بوابة الجودة `silk_quality_gate` (~130 فحصاً؛ الحاجزة في `FAIL_TRIGGER_CHECKS` :2548). السياسة الموحّدة `silk_export_gate.evaluate` :128.
التصدير: docx `api.report_docx` :3689 (client/academic/internal)، pdf `report_pdf` :3782 (docx ← LibreOffice `silk_reports.docx_to_pdf` :5222، فتحتان `SILK_PDF_MAX_CONCURRENT=2`)، md `report_md` :3854 **بلا بوابة**. الواجهة `web/index.html`: زر البحث :261 → readiness :790 → POST :830؛ أزرار PDF/Word/MD :1282–1291؛ لا تمرر `style` ولا `internal`.

## ٢. مسارات تنتهي بلا ملف للعميل (الرصيد المُنفَق = رصيد المشغّل لدى Anthropic؛ لا خصم عميل يُسترد لأن لا خصم أصلاً)

| المسار | الموضع | ما أُنفق | البديل |
|---|---|---|---|
| إيقاف مبكر (أعمدة ناقصة) | pipeline :596 | كل البعثات | استئناف بعد إكمال المصادر |
| سقف الإنفاق قبل الكاتب | pipeline :613 | البعثات + المحلل | إعادة توليد بعد رفع السقف |
| فشل نداء المحلل | pipeline :625 | البعثات | `POST /analyses/{id}/report` |
| كاتب معطّل / مسودة فارغة | `write_reviewed_report` :3406/:3421 | كل شيء | نقطة تفتيش `writer_partial` :718 |
| AI غير متاح | pipeline :689 | البعثات | لا شيء |
| استثناء/انقطاع مزوّد | api :3113–3126، :2208–2215 | الفعلي حتى لحظته | استئناف يدوي |
| بوابة الجودة FAIL (409) | `_block_client_export_if_gate_failed` :2016؛ خطأ البوابة نفسه = FAIL (`silk_export_gate` :146–155) | التشغيلة كاملة | `?override=1` بمفتاح مالك؛ `?internal=1` |
| `ClientArtifactGateError` | docx :3750 / pdf :3827 → 409 | كاملة | كما أعلاه |
| `RuntimeError` في docx | :3758–3776 → 501 | كاملة | لا شيء |
| PDF مشغول/محرّك غائب/فشل تحويل | :3833–3850 → 503 | كاملة | الواجهة تقترح Word فقط |
| Markdown `RuntimeError` | :3870–3880 → 501 | كاملة | لا شيء |

في التشغيل المرجعي المحاكى: docx وpdf = 409، md = 200 (البوابة لا تحكم md)، internal = 200.

## ٣. إجابات القسم (ب)

الأرقام
1. ما يحسبه النموذج بنفسه (برومبت `deep_report`): سعر الوحدة = مبلغ÷كمية (:1814–1819)؛ سعر الوحدة في جدول المنافسين ومركز «سعرك» (:2226–2255)؛ مسار الواردات سنة بسنة وسنة الانكسار وعدّ السنوات (:1793–1804)؛ CAGR من أول سنة مرصودة وقراءة بديلة (:1834–1839)؛ نسبة تغيّر الصرف (:2280)؛ «نطاق حصة واقعية» للسنوات 1–3 وإيرادها (:2036–2038) **محسوب بالكامل بالنموذج**؛ هامش مطابقة السعر (:2295)؛ قاعدة عامة «أظهر الحساب متى توفرت المدخلات» (:2329). الكسور اللفظية: **لا قاعدة**، والعقد يفضّل المقارنات الملموسة على النسب (`silk_style_contract.py:1178`). محسوب حتمياً: TAM/SAM/SOM، الحل العكسي EXW، أرقام القرار، HHI/النمو/CAGR/حصة السعودية كحقائق دفتر.
2. تفكيك القيمة إلى كمية وسعر: **غير موجود** (`silk_trend.growth_pct` :42، `silk_deep_pillars` ~826 على القيمة فقط). لا استبعاد لسنوات الوزن الشاذ؛ الحارس الوحيد: سعر حدود > 5× سعر الرف يُسقط من المقارنة (`silk_economics.py:1910–1932`) وتحذير `_check_border_price_out_of_range` :4097.
3. HHI: `silk_economics.hhi` :26 على كل حصص الشركاء؛ السنة = الممرَّرة وإلا **السنة الحالية−1** بلا تحقق من اكتمالها (`silk_llm_runtime.py:93–96`)؛ صيغة ثانية غير قانونية 0–1 في `silk_market_ranker.py:613`. **لا فحص اتساق مع الحصص المعروضة** (الفحوص: مقياس :2698، دقة زائفة :875، وحدة ~6743).
4. مرجع سعر الرف: `_pick_shelf_anchor` :1497 يختار **الأرخص** لكل كجم/لتر داخل مجموعة عملة؛ **لا يراعي الشريحة**. الشريحة موجودة فقط في عوامل SAM (`silk_research._TIER_FACTORS` :336).
5. سعر الحدود مرجعاً: نعم — إيراد SOM = سعة×12×سعر الحدود (`silk_research.py:483–494`)؛ هامش عند الحدود (:962–967)؛ فحص تناقض EXW < 0.8×المتوسط يفرض تحذير «ليس أساساً تفاوضياً» (`silk_economics.py:1933–1943`)؛ البرومبت يمثّل به لسعر الوحدة (:1816).
6. مكتمل/أولي: فقط «السنة التقويمية الحالية = جزئية» (`silk_deep_pillars._is_partial_year` :360)؛ لا التقاط لحالة provisional من كومتريد؛ `_recent_years` يفترض السنة−1 مكتملة.

الاتساق
7. تعارض تقريري/متحفظ بين الأقسام: **لا آلية**. قواعد برومبت فقط (:2076، :2099–2104) وفحصان تحذيريان ضيقان (`_check_epistemic_verb_discipline` :3255 للحل العكسي فقط؛ `_check_caveat_repetition` :5998 لعبارة واحدة). المصطلحات: قاعدة «مصطلح واحد» في العقد :1149 وفحصان تحذيريان (:5411، :5976). **تعارض داخلي:** العقد وsanitize يستبدلان HHI بـ«مؤشر تركّز السوق» (:1340؛ `silk_reports.py:3058`) بينما «مؤشر تركّز» محظور في مفردات العميل (`silk_quality_gate._CLIENT_VOCAB_TERMS` ~6230).
8. المصادر: `_client_references_section` (`silk_reports.py:4472`). «ما لم يتسنّ توثيقه»: `_client_gaps_section` :4253 ← `_client_gap_inputs` :3665 + ملاحظات منهجية البوابة. الجملة الختامية: `silk_i18n.gaps_none` :710 تُطبع فقط حين تخلو كل القوائم (:4314) ويحرسها `gaps_closing_contradiction` (حاجز).
9. العناصر النائبة: **يمكن أن تتسرب.** `placeholder_leak` (:417) يفحص 4 سلاسل نظام فقط (:337–342)؛ `_check_template_interpolation` :4983 يلتقط `{label}` والأقواس الفارغة؛ `_client_assert_clean` والـCSV يرفضان `{{ }}` فقط. لا التقاط لـ`[...]` أو «يُدرج» أو TODO. **والبرومبت نفسه يحمل قوالب** «[سمِّ المدخل من السطر أعلاه]» (:1977–1980) و«<الحكم المحسوب>» (:2361).
10. قرار مفروض: الحكم مقفول من `synthesize` ويُمنع الكاتب من مخالفته (:1666–1675). داخل ذلك: فقرة صوت نموذجية تنتهي «التوصية تميل نحو الدخول المشروط» (:2175–2177)؛ كتلة الإزاحة تقول «إن كان الجواب لا فالحكم يجب أن يعكسه» (:2040) متناقضة مع القفل. لا قالب قرار رباعي (دخول/مشروط/إرجاء/عدم دخول) بصياغة الدراسة.
11. نوع المصدّر: **غير معروف للكاتب**. `product_card` = تكلفة/وحدة/سعة/ثوابت/شكل/عملة/تمويل/شريحة (`silk_economics.py:1948–2037`)؛ البرومبت يفترض «المصدّر السعودي».
12. قواعد خاصة مدفونة: في برومبت الكاتب — طحينة/حلاوة وحليب سائل/بودرة (:1655)؛ المراعي/طلبات/كارفور (:1858)؛ رمضان/حلال (:2210، :2218، :2261، :2272)؛ «مشترون أوروبيون» (:2320)؛ سنوات إصدار LPI صلبة (:2118–2122) رغم قاعدة «لا تسمية لأحدث سنة»؛ في العقد: حليب مجفف (:1186)، فئات القهوة (:432، :574).

البيانات
13. فشل المؤشرات العامة: مهلة اتصال 10ث/قراءة 45ث؛ قاطع دائرة لكل مضيف يرفع `CircuitOpen` بلا نداء (:207)؛ WGI يحتاج `source=3` وإلا «archived» (:913)؛ الصرف يستسلم بعد فشل واحد؛ المخزن لا يُملأ من البعثات. المعاملة: فجوة معلنة `value=None`؛ لكن `status` يُملأ فقط في مسار واحد (:964–967)، والبقية `""` وتُصنَّف بمطابقة كلمات؛ تغليف WGI يُسقط السنة. في التشغيل المرجعي ظهرت 8 محاولات World Bank فاشلة (WGI×3، FX×2…) كلها «تعذّر الجلب — أعد المحاولة» بلا إعادة محاولة تلقائية.
14. قدرة الالتقاط الميداني: مقتطفات بحث ويب ثقة 0.4 + بحث LLM في `pricing_scout` (زوايا `silk_missions.py:52`)؛ لا استخراج مهيكل للشريحة ولا لشروط المنافذ ولا لدور الجهة.

التسليم والتكلفة
15. المراجعة: موجودة (س٧ أعلاه)؛ معيارها: رقم بلا مصدر، تناقض، ادعاء غير مسند، ترتيب الأقسام، وحدات السعر (:3104–3125). جولة واحدة افتراضاً، بلا معيار درجة، بلا سقف تكلفة خاص.
16. انظر الجدول §٢. الرصيد المُنفَق يبقى في كل الصفوف.
17. فحص كفاية قبل الخصم/التوليد: **لا**؛ الأقرب هو الإيقاف المبكر **بعد** إنفاق البعثات كلها.
18. التكلفة: حجز 3.0$؛ الوثائق: ~$1.6–2.0 (`DEEP_RESEARCH_DECISIONS.md:1538`، :4374)، ذروة ~$7 (:1216)، $1.80 لتشغيلة (:3814)، ~20 دقيقة/تشغيلة (`docs/SPEC-v2.md:32`). مسجَّل لكل تشغيلة: `cost_usd_estimate`، `cost_usd_by_model`، `cost_usd_by_mission`، `stage_seconds` (`silk_research_pipeline.py:762–774`). **غير مسجّل:** تكلفة الكاتب مقابل المراجع منفصلتين، ولا تكلفة التصدير، ولا درجة جودة. التشغيل المحاكى: 0.7ث، 0$، 24 نداء LLM مزيف، 0 نداء أداة.

الاختبارات
19. 429 ملف اختبار. `pytest` كان غائباً (`No module named pytest`)، فحص الإيقاف الأحمر كان لهذا السبب. بعد التثبيت: تشغيلة `-x`: 929 نجاح ثم فشل `tests/test_factory_report_language.py::test_final_pdf_artifact_is_in_the_factory_language[ar|en]` — سببه بيئي (LibreOffice بلا writer: «source file could not be loaded» حتى لملف نصي؛ صورة النشر تثبّت `libreoffice-writer`). الاختبار لا يتخطّى عند محرّك موجود لكن معطوب (ملاحظة منخفضة). السويت الكامل بعد استبعاده: **6028 نجاح، 73 تخطٍّ، 7 فشل** — السبعة كلها تحويل PDF بالسبب البيئي نفسه (`test_pdf_bracket_pairing`×4، `test_platform_pivot_bridge`، `test_report_output_overhaul`، `test_wave_z_followups`). بعد تثبيت `libreoffice-writer` و`poppler-utils` (pdftotext) و`fonts-hosny-amiri` في الحاوية (الثلاثة في `Dockerfile:22` لصورة النشر): التسعة خضراء، وبقي اختباران في `test_wave2_first_pdf_cluster.py` فشلا لغياب الخط العربي ثم اخضرّا بعد تثبيته؛ الحصيلة النهائية (تشغيلة كاملة واحدة) **6045 نجاح، 65 تخطٍّ، 0 فشل** — لا فشل في الكود؛ الفشل كله من نقص أدوات الحاوية مقارنة بصورة النشر (ملاحظة تشغيلية: يلزم إضافة الحزم الثلاث + pytest إلى إعداد بيئة الجلسة السحابية كي لا يحمرّ حارس الإيقاف زوراً). مناطق ضعيفة التغطية: `silk_itc_tariff` (1)، `_wgi_governance_datapoints` (1)، `silk_imf_agent` (3)، `silk_channels_agent` (3)، `silk_competitors_agent` (3)؛ لا اختبار لبقاء `data_year` عند سقوط سنة البنك الدولي؛ لا حالة ذهبية قهوة/ماليزيا؛ لا مشغّل هرمتي لتشغيلة بعثات حقيقية.

## ٤. الحالة المرجعية: التشغيل والمقارنة

التشغيل: 090121 → ماليزيا، نمط `decision`، شبكة مقطوعة، `_call_tools`/`_call` محاكاة (نمط `tests/test_cost_speed_e.py:79–110`)، سكربت `scratchpad/baseline_my_coffee.py`. الحي متعذّر هنا (لا مفاتيح، سياسة شبكة) — موثَّق.
- الطلب الأول رُفض 422 (بوابة HS) حتى `hs_confirmed=true`.
- النتيجة: `completed` في 0.7ث، `early_halt=true`، 12/12 بعثة فجوة، الحكم «NO-GO (insufficient data)»، docx/pdf 409، md 200 (15 KB من إطار حتمي: ترويسة، «الحكم وأساسه»، نموذج اقتصادي فارغ، جدول مؤشرات مالية «يتطلب الحساب»، أثر المصادر، أسئلة حاسمة، حدود، مختصر).
- التكلفة 0$ (محاكاة). تقدير الحي من الوثائق: ~$2 و~20 دقيقة.

درجة خط الأساس (بمعيار المرجع 9.5/10):
- التشغيل المحاكى: **1/10** — يثبت الأنابيب فقط؛ لا سرد ولا جداول من الخمسة ولا أقسام من الأحد عشر.
- أحدث شاهد حي في الريبو (`samples/kuwait_peanut_butter_research_report.md`، طبعة 2026-07-21 موسومة «ما قبل الإصلاح»): **3.5/10** — أقسام ١١ بعناوين مختلفة، نقاط تعداد، مفردات داخلية (HHI، CAGR، Trends، «تشغيل»)، ضمير المتكلم («لم نعرفه بعد»)، حكم بوسم خوارزمي، جداول اقتصادية بأرقام افتراضية معلنة، لا جدول متطلبات/جهات/خطة.
- **مخرَج الكاتب الحي الحالي: no sufficient evidence** — لا عيّنة حية بعد إصلاحات أغسطس–سبتمبر في الريبو (`research_report_latest.md` نص مموّه مكتوب يدوياً في `tools/gen_research_sample.py:25`). يلزم تشغيل حي واحد (~$2) بعد الموافقة لتثبيت الدرجة.

## ٥. جدول النتائج

الشدة: ح=حرجة، ع=عالية، م=متوسطة، خ=منخفضة. الأثر: مطابقة/تسليم/تكلفة.

| # | المشكلة | الدليل | الفئة | الأثر | الشدة |
|---|---|---|---|---|---|
| F-01 | تقرير مدفوع يُحجب كلياً عن العميل عند FAIL البوابة (409 docx/pdf) بلا بديل إلا مفتاح المالك | `api.py:2016–2060`، `silk_export_gate.evaluate` :128 | تسليم | تسليم | ح |
| F-02 | خطأ داخلي في البوابة يُعامَل FAIL فيحجب تقريراً سليماً | `silk_export_gate.py:146–155` | تسليم | تسليم | ح |
| F-03 | لا فحص كفاية بيانات قبل الحجز الدولاري؛ الإيقاف المبكر بعد إنفاق كل البعثات | `api.py:2936`، `pipeline:337–352` | تكلفة | تكلفة | ح |
| F-04 | `SILK_RESEARCH_MAX_USD` غير مضبوط = بلا سقف للتشغيلة؛ الحارس لا يجهض نداءً جارياً | `pipeline:259–289` | تكلفة | تكلفة | ح |
| F-05 | فشل PDF (503) بلا تسليم docx تلقائي؛ الواجهة تقترح Word يدوياً | `api.py:3833–3850`، `index.html:1282` | تسليم | تسليم | ع |
| F-06 | md يتجاوز بوابة العميل بينما docx/pdf محجوبان — سلوك غير متسق | `api.py:3854` (لا استدعاء للبوابة) | تسليم | تسليم | م |
| F-07 | لا مهلة كلية للتشغيلة الخلفية | `api.py:2181`، `:2153` orphan فقط | تسليم | تسليم | ع |
| F-08 | فشل جلب المنافسين يعطّل احتياط المرآة وينهار بصمت (`None.get`) | `silk_data_layer_v2.py:300` ↔ `silk_llm_runtime.py:256,283`؛ `silk_missions.py:754` | أرقام | مطابقة | ع |
| F-09 | لا تفكيك لنمو القيمة إلى كمية وسعر، ولا استبعاد لسنوات الوزن الشاذ | `silk_trend.py:42`، `silk_deep_pillars.py:~826` | أرقام | مطابقة | ع |
| F-10 | HHI بلا فحص اتساق مع الحصص؛ السنة = الحالية−1 بلا تحقق؛ صيغة ثانية غير قانونية | `silk_llm_runtime.py:93–96,283`، `silk_market_ranker.py:613` | أرقام | مطابقة | ع |
| F-11 | مرجع سعر الرف = الأرخص، لا شريحة | `silk_economics.py:1497–1544` | أرقام/منطق | مطابقة | ع |
| F-12 | سعر الحدود يدخل SOM والهامش ويُمثَّل به في البرومبت | `silk_research.py:483–494,962`، `silk_ai_judge.py:1816` | أرقام | مطابقة | ع |
| F-13 | لا حالة «أولي/مكتمل» في DataPoint؛ السنة الجزئية = التقويمية الحالية فقط | `silk_data_layer.py:350–391`، `silk_deep_pillars.py:360` | أرقام | مطابقة | ع |
| F-14 | النموذج يحسب: سعر الوحدة، CAGR، تغيّر الصرف، حصة إزاحة وإيرادها، عدّ السنوات | `silk_ai_judge.py:1793–1839,2036–2038,2280,2295` | أرقام | مطابقة | ح |
| F-15 | لا قاعدة ولا فحص للكسور اللفظية؛ فحص الإسناد أرقام لاتينية فقط | `silk_evals.py:58,92`؛ `silk_style_contract.py:1178` | أرقام | مطابقة | ع |
| F-16 | لا آلية لاتساق حالة الادعاء بين الأقسام | `silk_quality_gate.py:3255,5998` (ضيقان تحذيريان) | اتساق | مطابقة | ع |
| F-17 | تعارض مصطلح HHI: العقد/sanitize يفرضان «مؤشر تركّز السوق» والمفردات تحظره | `silk_style_contract.py:1340`، `silk_reports.py:3058`، `silk_quality_gate.py:~6230` | اتساق | مطابقة | م |
| F-18 | عناصر نائبة قد تتسرب؛ البرومبت نفسه يحوي قوالب `[…]` و`<…>` | `silk_quality_gate.py:337–342,417`؛ `silk_ai_judge.py:1977–1980,2361` | تحفظ | مطابقة | ع |
| F-19 | صوت مثالي في البرومبت يميل إلى «الدخول المشروط»؛ كتلة الإزاحة تناقض قفل الحكم | `silk_ai_judge.py:2175–2177,2040–2042` | منطق | مطابقة | ع |
| F-20 | نوع المصدّر غير معروف للكاتب/المراجع | `silk_economics.py:1948–2037` | منطق | مطابقة | ع |
| F-21 | قواعد منتج/سوق مدفونة في البرومبت والعقد العام | `silk_ai_judge.py:1655,1858,2118–2122,2210,2320`؛ `silk_style_contract.py:432,574,1186` | تعميم | مطابقة | م |
| F-22 | تعارضات داخل عقد الأسلوب (طول الجملة 15–20/12–25/≤25؛ صوت التقرير مقابل المتكلم؛ عملة؛ نقاط؛ bold؛ تكرار الرقم) | `silk_style_contract.py:1144,1182,1187,1323`؛ `silk_ai_judge.py:2082,2125,2146,2158,2231,2374` | أسلوب | مطابقة | ع |
| F-23 | الأقسام ١١ الحالية وخريطة الدمج إلى ٦ لا تطابق هيكل المرجع؛ لا جداول من الخمسة | `silk_ai_judge.py:821`، `silk_reports.py:3358` | هيكل | مطابقة | ح |
| F-24 | لا فحص لأعمدة الجداول ولا لنقاط التعداد خارج الجداول ولا للمحظورات الأسلوبية | `silk_quality_gate.py:4512,4614`؛ `silk_evals.structural_checks` :485 | أسلوب | مطابقة | ع |
| F-25 | تغليف WGI يُسقط السنة/الحالة؛ سعر الوحدة بلا سنة؛ سقوط السنة عند بديل البنك الدولي | `silk_missions.py:540–548`؛ `silk_llm_runtime.py:199–204`؛ `silk_data_layer.py:906–911` | بيانات | مطابقة | م |
| F-26 | المؤشرات العامة تفشل بلا إعادة محاولة تلقائية (الصرف يستسلم بعد فشل واحد) وتُعلن فجوة | `silk_missions.py:651`؛ `silk_data_layer.py:207` | بيانات | مطابقة | ع |
| F-27 | لا استخراج مهيكل لجهات بالاسم/دورها/شروط المنافذ/شريحة السعر | `silk_channels_agent.py`، `silk_missions.py:52` | ميداني | مطابقة | ع |
| F-28 | جدول المتطلبات لا يعرض `verified_at` ولا يوسم القديم ولا يعرض «يُستوضح» | `silk_requirements_agent._row_dp` :364؛ لا مستهلك للعمود في `silk_reports.py` | حراسة | مطابقة | م |
| F-29 | المراجعة جولة واحدة بلا درجة ولا معيار مكتوب ولا اختيار أفضل إصدار | `silk_ai_judge.py:3265,3340,3515` | مراجعة | مطابقة/تكلفة | ع |
| F-30 | تكلفة الكاتب/المراجع/التصدير غير مفصولة ولا درجة مسجّلة | `silk_research_pipeline.py:762–774` | تكلفة | تكلفة | م |
| F-31 | بوابة HS ترفض الاسم الكامل للمنتج رغم صحة الرمز (صفة «غير منزوعة الكافيين») | `silk_hs_confirm.preflight_block` :869؛ `data/hs_codes.csv:34` | تسليم | تسليم | م |
| F-32 | لا قائمة مراكز إعادة تصدير؛ القاعدة `imports>=exports` فقط | `silk_commercial_analysis.py:292,358` | أرقام | مطابقة | م |
| F-33 | قائمة «ما لم يتسنّ توثيقه» لا تضم ادعاءات «يُفاد» ولا الجهة القادرة على الاستكمال بشكل ممنهج | `silk_reports.py:3665,4253` | اتساق | مطابقة | م |
| F-34 | اختبار PDF لا يتخطّى عند محرّك موجود معطوب؛ pytest غائب في بيئة الجلسة | `tests/test_factory_report_language.py` | اختبار | — | خ |
| F-35 | لا حالة ذهبية قهوة/ماليزيا ولا مشغّل هرمتي لبعثات حقيقية | `evals/golden_set/`، `tools/golden_set.py` | اختبار | مطابقة | م |

## ٦. ملحق المقارنة بالمرجع (قسماً قسماً)

| قسم المرجع | المخرج المحاكى | الشاهد الحي (الكويت) | الفئة الغالبة |
|---|---|---|---|
| الترويسة (منتج+HS، منشأ، سوق، تاريخ، أحدث سنة مكتملة) | جدول ترويسة بلا «أحدث سنة مكتملة»؛ يحمل «ثقة التصنيف 0.0» و«الحكم» | نفسه + «مراجعة التقرير: 1 دورة» | هيكل/تحفظ |
| الملخص التنفيذي (توصية مشروطة + 4 متطلبات) | غائب (إيقاف مبكر) | حكم بوسم + ثقة 50% + HHI بشرح قوسي | هيكل/أسلوب |
| أولاً حجم السوق (سلسلة 6 سنوات + تفكيك كمية/سعر + حصص + HHI + سعر حدود موصوف) | غائب | رقم واحد + CAGR؛ لا سلسلة ولا تفكيك | أرقام/منطق |
| ثانياً الطلب (استطلاع موسوم كاسترشادي، نمطان، حلال، موسمية معلنة فجوة) | غائب | سطران | عمق |
| ثالثاً المنافسة والتسعير (جدول أسعار 4 أعمدة، محامص محلية، ميزة مشروطة بالجدوى، طزاجة) | غائب | موردون بالدول، سعر رف واحد، لا محليون | ميداني/منطق |
| رابعاً المتطلبات (جدول 4 أعمدة مع «آخر تحقق»، حلال/JAKIM، السؤال السابق على المدة) | غائب (الصفوف موجودة في CSV ولم تُستدعَ) | سطر عام | حراسة |
| خامساً القنوات (جدول جهات 3 أعمدة، استبعاد معلل، 2–3 مرشحين) | غائب | «لا جهات» | ميداني |
| سادساً المؤشرات المالية (قاعدة 40–45% موسومة تقديرية، عتبة مؤقتة) | جدول «يتطلب الحساب» بنبرة تعليمية | حل عكسي بافتراضات معلنة + جدول سيناريوهات | أسلوب/منطق |
| سابعاً المخاطر (صرف بتعريف، اقتصاد كلي، بن عالمي، حوكمة معلنة فجوة) | فجوات WGI/FX خام في «أثر المصادر» | سطر واحد | أرقام/بيانات |
| ثامناً الحجة المضادة وشروط انقلابها | غائب | غائب | منطق |
| تاسعاً خطة 90 يوماً (جدول 4 أعمدة مرتبط بالمتطلبات) | غائب | سطران مرقّمان | هيكل |
| المصادر وحدود الدراسة + جملة ختامية | «حدود هذا التقرير» جدول 3 أعمدة من نصوص البعثات الخام | كذلك + «أثر المصادر» | اتساق |

أخطاء البند ٨ الظاهرة في المخرجات الحالية: في الشاهد الحي: 8 (نصوص تشغيلية «لا رد نهائي من التحليل الآلي» في المحاكى، ووسوم خوارزمية)، 11 (HHI/«مؤشر تركّز»)، 12 (ثقة 50% بلا سند)، 15 (WGI/FX فجوة بلا إعادة محاولة — المحاكى)، 16 (لا جهات)، 17 (لا ميزة أصلاً)، 18 (HHI بشرح قوسي مكرر لا تعريف للتقلب)، 19 (عنوان «التقرير السردي الكامل» فوق فقرة اعتذار). **غير قابل للحكم بلا تشغيل حي:** 1، 2، 3، 4، 5، 6، 7، 9، 10، 13، 14 — لا كاتب حي في العيّنة.
