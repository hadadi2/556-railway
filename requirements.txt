# مثبَّتة بإصدارات محدّدة (M0) — pinned for reproducible builds; bump deliberately.
# Versions = the exact set proven green in CI on 2026-07-06.
requests==2.34.2
fastapi==0.139.0
uvicorn==0.50.0
python-docx==1.2.0
pytrends==4.9.2
# منصّة سِلك (silk_platform/): تجزئة كلمات المرور بـbcrypt عامل 12 (المواصفة §11).
# عجلة Rust مستقلّة (لا cffi)؛ حين تغيب يرجع الكود إلى hashlib.scrypt القياسي.
# Silk platform password hashing (bcrypt cost 12, spec §11); scrypt fallback if absent.
bcrypt==4.2.1
# DEBT-7 (تدقيق 2026-09-01): `api.py` يستورد pydantic مباشرةً (BaseModel/Field) بينما
# كانت تصل **ضمناً** عبر fastapi — اعتماديةٌ مستعملةٌ غيرُ معلَنة. الإصدارُ هو ما يحلّه
# CI اليوم مع fastapi 0.139.0 (تُحقّق منه 2026-09-06).
pydantic==2.13.5
# منصّة سِلك: تحليل multipart/form-data لرفع الصور الحقيقي (PR-8) — FastAPI
# يحتاجها فعلياً عند استعمال UploadFile/File/Form، وإلا يفشل عند التسجيل.
# Silk platform: multipart/form-data parser FastAPI's UploadFile/File/Form need.
python-multipart==0.0.31
# **حارسُ اتجاه الأقواس في الـPDF العربي (تدقيق 2026-08-27، البندان W-01/W-04).**
# `silk_reports._pdf_bracket_check` يقيس هندسةَ محارف الأقواس عبر `fitz`؛ بلا
# هذه الحزمة يرتدّ الفحصُ صامتاً («skipped») فتُسلَّم مستنداتٌ بلا أيّ حارس —
# نفسُ عائلة الخمود (الدرس ١٠٤: حارسٌ قائمٌ في الشيفرة غائبٌ عن كلّ مسارٍ يعمل).
# نُقِلت من وظيفة e2e وحدها إلى الإنتاج **بعد** معايرة المقياس، لا قبلها:
# المقياسُ القديم كان يرفض المستندَ السليم، فتفعيلُه كان سيُسقِط كلّ تصدير.
# PDF bracket-orientation guard needs pymupdf at runtime, or it silently skips.
pymupdf==1.28.2

# ITC public chart response transport.
cryptography==50.0.1
